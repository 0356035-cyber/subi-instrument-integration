# 临床研究 Sub-I 与仪器培训系统 — 系统总览

> 更新日期：2026-07-03  
> 适用环境：家里 / 单位 Windows、极空间 NAS（Z4 Pro）+ Cloudflare Tunnel 外网访问

---

## 1. 系统定位

本工作区部署的是**部门内部管理系统**，服务皮肤与化妆品研究室 Sub-I 团队日常运营。

| 系统 | 定位 |
|------|------|
| **Sub-I 临床研究资料库** | 法规/模版/项目资料归档、项目管理、排班日历、资质查询 |
| **仪器培训与授权系统** | 人员培训、考核、仪器授权及到期管理 |

**不是完整 LIMS。** 实验室样品流、仪器原始数据采数等仍用专业工具；系统负责项目运营、资料归档、人员授权与排班协调。

**分工原则：** 系统负责「找得到、挂得住、追得回、排得清」；Word / Excel / SPSS 负责专业撰写与统计，定稿与结果回系统归档。

---

## 2. 整体架构

```
                         ┌──────────────────────────┐
                         │       用户浏览器          │
                         └────────────┬─────────────┘
                                      │
         ┌────────────────────────────┼────────────────────────────┐
         │                            │                            │
  Streamlit :8501              Streamlit :8510              Cloudflare Tunnel
  仪器培训管理界面              Sub-I 资料库界面              （极空间外网 HTTPS）
         │                            │
         ▼                            ▼
  FastAPI :8000                 FastAPI :8001
  instrument-training-home      subi_knowledge_platform
  （培训 API）                   （Sub-I API + BFF 代理）
         │                            │
         │◄────── 服务账号 JWT ─────────┘
         ▼
  SQLite（各自独立数据库文件）
```

### 2.1 四个 Docker 容器

| 容器名 | 服务 | 本地端口 | 说明 |
|--------|------|----------|------|
| `instrument_train_backend` | 培训 API | 8000 | 不对外网暴露 |
| `instrument_train_frontend` | 培训管理页面 | 8501 | 用户主入口 |
| `subi_backend` | Sub-I API | 8001 | 含资质查询 BFF |
| `subi_frontend` | Sub-I 页面 | 8510 | 用户主入口 |

极空间上 API 端口可能映射为 `18000` / `18001`（避免与极空间自带服务冲突）。页面端口仍为 `8501` / `8510`。

可选第五容器：`cloudflared`（Cloudflare Tunnel），见 `docker-compose.tunnel.yml`。

### 2.2 设计要点

- Sub-I 前端**只访问 8001**，不直连培训系统 8000
- 培训系统服务账号保存在 `subi_knowledge_platform/.env`，**不暴露给浏览器**
- 两系统以**工号 `employee_id`** 关联人员
- 资料多版本以 **`document_group_id`** 归组

---

## 3. 代码仓库与目录

### 3.1 三个独立 Git 仓库

| 仓库 | GitHub | 内容 |
|------|--------|------|
| `subi-instrument-integration` | [subi-instrument-integration](https://github.com/0356035-cyber/subi-instrument-integration) | Compose 编排、启动/备份脚本、交接文档 |
| `subi_knowledge_platform` | [subi_knowledge_platform](https://github.com/0356035-cyber/subi_knowledge_platform) | Sub-I 后端 + 前端 |
| `instrument-training-home` | [instrument-training-home](https://github.com/0356035-cyber/instrument-training-home) | 仪器培训与授权系统 |

### 3.2 工作区目录结构

```
projects/                              ← 整合仓库根目录
├── docker-compose.yml                 ← 四容器统一部署
├── docker-compose.tunnel.yml          ← Cloudflare Tunnel 叠加
├── docker-compose.zspace.yml          ← 极空间兼容叠加（可选）
├── 系统总览.md                         ← 本文档
├── 项目整合交接文档.md                 ← 详细交接与产品规划
├── 极空间部署说明.md
├── 回家部署清单.md
├── README.md
├── 启动系统.bat / start-docker.bat
├── 备份数据.bat / restore-data.bat
├── backups/                           ← 备份与离线部署包（不提交 Git）
├── instrument-training-home/
│   ├── data/instrument_training.db    ← 培训业务数据
│   └── streamlit_app/                 ← 培训管理页面
└── subi_knowledge_platform/
    ├── data/subi_knowledge.db         ← 资料库数据
    ├── uploads/                       ← 资料附件
    └── frontend/                      ← Sub-I 页面
```

**同步原则：** 代码用 `git pull`；业务数据用 `备份数据.bat` / `恢复数据.bat`。

---

## 4. Sub-I 资料库（:8510）

技术栈：Streamlit + FastAPI + SQLite。

### 4.1 功能模块（已上线）

| 模块 | 功能 |
|------|------|
| **通用资料库** | 首页分类、资料列表、上传/编辑/删除、附件下载 |
| **版本管理** | 版本组、历史版本、元数据+正文左右对比 |
| **项目管理** | 项目台账、标签筛选、相似项目推荐 |
| **资料挂项目** | `project_id` 关联，列表按项目筛选 |
| **项目日历** | 访视节点、基线日自动生成日历事件 |
| **周排班** | 员工填报、上下午分栏、草稿续填、Excel 导出、部门汇总 |
| **资质查询** | 按工号 / 按仪器 / 批量核验（代理培训系统 API） |
| **用户管理** | admin 管理账号与角色 |

### 4.2 侧边栏导航

- 资料库首页、资料列表
- 项目管理、项目日历、周排班
- 上传资料（editor / admin）
- 用户管理（admin）
- 项目检测人员授权资质查询

### 4.3 角色权限

| 角色 | 能力概要 |
|------|----------|
| `viewer` | 只读资料 |
| `editor` | 上传、编辑资料；项目管理与排班 |
| `admin` | 全部权限 + 用户管理 |

更细的总库权限、内部要求可见标识、主任修改提醒等规则见 `项目整合交接文档.md` §14.12（部分持续迭代中）。

---

## 5. 仪器培训系统（:8501）

技术栈：Streamlit + FastAPI + SQLite。

### 5.1 功能模块（已上线）

| 模块 | 功能 |
|------|------|
| **人员管理** | 科室人员登记；显示「可登录」状态 |
| **仪器管理** | 台账、重复仪器合并 |
| **培训记录** | 录入与查询（须先选人员，不默认展示全员） |
| **考核记录** | 录入、筛选；考核通过可跳转授权 |
| **授权管理** | 授权列表、新增、待授权、权限查询 |
| **人员综合查询** | 下拉选人；按仪器关联展示培训/考核/授权总表 |
| **到期提醒** | 授权即将到期预警 |
| **操作日志** | 增删改及授权操作留痕 |
| **Excel 导入** | 批量导入实验室 Excel |
| **统计报表** | 培训/考核/授权统计 |

### 5.2 登录白名单

科室可登记多名人员，但**仅以下 4 人可登录**（`can_login`，容器启动时按配置名单同步）：

- 王建玟、江文才、谈益妹、郭梦格

配置位置：`instrument-training-home/app/config.py` → `login_allowed_names`。

### 5.3 角色权限

| 角色 | 能力概要 |
|------|----------|
| **管理员** | 全部管理功能 |
| **培训负责人** | 培训/考核/授权管理、综合查询、到期提醒等 |
| **普通人员** | 仅查看本人培训/考核/授权记录（只读） |

---

## 6. 两系统整合

```
Sub-I 浏览器 (:8510)
    → Sub-I 后端 (:8001) Bearer Token
        → 培训 API (:8000) 服务账号 JWT
            → instrument_training.db
```

**培训系统对外整合 API（供 Sub-I 调用）：**

| 接口 | 说明 |
|------|------|
| `GET /api/v1/integrations/qualifications` | 按工号查单人资质 |
| `GET /api/v1/integrations/qualifications/by-instrument` | 按仪器查人员资质 |
| `GET /api/v1/users/by-employee-id/{employee_id}` | 按工号查用户 |
| `GET /health` | 健康检查 |

服务账号环境变量见 `subi_knowledge_platform/.env.example`。

---

## 7. 部署环境

### 7.1 家里 / 单位 Windows

| 操作 | 脚本 |
|------|------|
| 启动 | `启动系统.bat` 或 `start-docker.bat` |
| 备份 | `备份数据.bat` |
| 恢复 | `恢复数据.bat` |
| 定时备份（可选） | `安装定时备份.bat`（每日 02:00，保留 14 份） |

| 访问 | 地址 |
|------|------|
| 培训系统 | http://127.0.0.1:8501 |
| Sub-I 资料库 | http://127.0.0.1:8510 |

### 7.2 极空间 NAS

| 项目 | 说明 |
|------|------|
| 运行目录 | `/tmp/zfsv3/sata11/13816937329/data/docker/projects` |
| 内网示例 | `http://192.168.50.56:8501`、`http://192.168.50.56:8510` |
| 外网 | Cloudflare Tunnel（`cloudflared` 容器） |
| 外网域名示例 | `https://train.wangjm0206.top`、`https://subi.wangjm0206.top` |
| 详细步骤 | 见 `极空间部署说明.md`、`回家部署清单.md` |

极空间通常**无 git**，更新方式：从家里拷贝变更的代码目录 → SSH 执行 `docker compose build` → `docker compose up -d`。勿覆盖 `data/` 中的数据库文件。

### 7.3 单位离线机

| 步骤 | 在哪里 | 脚本 |
|------|--------|------|
| 1. 打包 | 家里（Docker 已启动） | `导出离线部署包.bat` |
| 2. 拷贝 | U 盘 | 整个 `backups/offline-deploy_*` 文件夹 |
| 3. 部署 | 单位 | 包内 `离线部署.bat` |

---

## 8. 数据与备份

| 数据 | 路径 |
|------|------|
| 培训数据库 | `instrument-training-home/data/instrument_training.db` |
| 资料库数据库 | `subi_knowledge_platform/data/subi_knowledge.db` |
| 资料附件 | `subi_knowledge_platform/uploads/` |
| Tunnel Token | `.env.tunnel`（不提交 Git） |

---

## 9. 日常运维速查

| 场景 | 操作 |
|------|------|
| 每天开机 | `启动系统.bat` |
| 关机前 | `备份数据.bat` |
| 拉最新代码 | 三仓库分别 `git pull origin main` |
| 更新本地容器 | `docker compose up -d --build` |
| 极空间更新 | SSH → 覆盖代码 → `docker compose build` → `up -d` |
| 启动外网 Tunnel | `启动Tunnel.bat`（极空间） |
| 换电脑 | `备份数据.bat` → 拷贝 `backups/` → 新电脑 `恢复数据.bat` |

**默认登录：** 工号 `3267`，密码 `123456`（部署后请立即修改）。

---

## 10. Clinical Research OS 2.0（架构蓝图）

> **现状：1.0 稳定运行；2.0 为演进蓝图，不立即重写系统。**

从「资料库中心」升级为 **「以 Project 为中心的项目操作系统」**：

```
Project 项目
├── Documents / Datasets / Analysis / Reports
├── Schedule / Staff & Qualification
└── AI Workspace
```

| 文档 | 内容 |
|------|------|
| [CODE_MAP.md](./CODE_MAP.md) | 代码地图：文件/API/表/页面索引 |
| [ARCHITECTURE.md](./ARCHITECTURE.md) | 2.0 架构、两系统角色、三层分离 |
| [DATA_MODEL.md](./DATA_MODEL.md) | 六大核心对象 |
| [DEVELOPMENT_ROADMAP.md](./DEVELOPMENT_ROADMAP.md) | 阶段 1～8 路线图（**当前：阶段 3**） |
| [CODEX_WORKFLOW.md](./CODEX_WORKFLOW.md) | 后续开发协作约定 |

**不变：** Docker 四容器；`instrument-training-home` 独立；BFF 资质代理。

---

## 11. 产品边界与后续规划

### 11.1 已明确不做

- 完整 LIMS（样品全生命周期、仪器直采等）
- 拖拽式甘特图 / MS Project 级项目管理
- 另建第三套独立子系统（当前为总库合并架构）

### 11.2 规划中 / 迭代中

- **2.0 路线图：** 见 [DEVELOPMENT_ROADMAP.md](./DEVELOPMENT_ROADMAP.md)
- **1.0 归档讨论：** `项目整合交接文档.md` §14（权限 §14.12 仍有效）

### 11.3 外用工具分工

| 能力 | 归属 |
|------|------|
| 项目信息、资料归档、排班、操作日志 | **本系统** |
| Word 排版定稿 | **Microsoft Word** |
| 复杂统计 | **SPSS / GraphPad / R** |
| 培训/考核/授权台账 | **instrument-training-home** |

---

## 12. 相关文档索引

| 文档 | 用途 |
|------|------|
| **系统总览.md** | 本文档：1.0 现状一页总览 |
| **CODE_MAP.md** | 2.0 代码地图（改哪 / 不该改哪） |
| **ARCHITECTURE.md** | 2.0 架构蓝图 |
| **DATA_MODEL.md** | 2.0 核心数据模型 |
| **DEVELOPMENT_ROADMAP.md** | 2.0 阶段路线图 |
| **CODEX_WORKFLOW.md** | AI/Codex 开发约定 |
| **README.md** | 快速上手与脚本说明 |
| **项目整合交接文档.md** | 整合细节、§14/§14.12 |
| **极空间部署说明.md** | NAS + Cloudflare 完整实施 |
| **回家部署清单.md** | 极空间 SSH 操作清单 |
| **subi_knowledge_platform/项目交接总结.md** | Sub-I 页面与接口细节 |

---

## 13. 新对话快速上手

```text
请阅读 projects/系统总览.md（1.0 现状）、CODE_MAP.md（代码落点）、DEVELOPMENT_ROADMAP.md（当前阶段），
在此基础上继续维护 Sub-I 与仪器培训系统；默认渐进演进，不整体重写。
Docker 启动：启动系统.bat 或 start-docker.bat
访问：培训 http://127.0.0.1:8501 | Sub-I http://127.0.0.1:8510
极空间路径：/tmp/zfsv3/sata11/13816937329/data/docker/projects
默认账号：工号 3267，密码 123456
```

---

*一句话：一套 Docker 四容器、两个业务系统、三个 Git 仓库——Sub-I 管资料/项目/排班，培训系统管人员授权；极空间 + Cloudflare 实现无公网 IP 外网访问。*