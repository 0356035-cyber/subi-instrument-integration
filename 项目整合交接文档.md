# Sub-I 与仪器培训系统整合 — 项目交接文档

> 更新日期：2026-07-03（第十轮：周排班上下午分栏、Excel 导出、草稿续填、部门汇总权限）
> 交接范围：`subi_knowledge_platform` + `instrument-training-home` API 整合与培训系统日常管理功能

---

## 1. 整合目标与成果

将 **Sub-I 临床研究资料库** 与 **仪器培训授权系统（instrument-training-home）** 打通，并持续完善资料库版本管理能力。

| 模块 | 状态 |
|------|------|
| 资质查询（按工号 / 按仪器 / 批量核验） | **已上线可用** |
| 仪器下拉搜索 + BFF 代理 | **已上线可用** |
| 资料版本组 `document_group_id` | **已上线可用** |
| 独立资料详情页 + 历史版本查看/下载 | **已上线可用** |
| 版本对比（元数据 + 正文左右对照 + 页码定位） | **已上线可用** |
| 生效日期 `date_input` 统一校验 | **已上线可用** |
| Sub-I Streamlit 前端容器化（:8510） | **已上线可用** |
| 培训/考核/授权 ID 按日期重排 | **已上线可用** |
| 授权多次登记 + 最新授权日期生效 | **已上线可用** |
| 仪器管理去序列号/位置 + 重复仪器合并 | **已上线可用** |
| 培训/考核/授权单日筛选 | **已上线可用** |
| 项目管理（台账 + 标签筛选 + 相似项目） | **一期已上线** |
| 资料挂项目 `project_id`（上传/编辑/列表筛选） | **一期已上线** |
| 排期排班（访视节点、月历、周排班） | **阶段 0～2 已上线** |
| 周排班上下午分栏 + Excel 导出 + 草稿续填 | **2026-07-03 已上线** |

---

## 2. 整体架构

```
用户浏览器 (Streamlit :8510)
        ↓ Bearer Token（Sub-I 用户登录态）
Sub-I 后端 (FastAPI :8001)          ← BFF 代理层，隐藏培训系统服务账号
        ↓ 服务账号 JWT
instrument-training-home (FastAPI :8000)
        ↓
SQLite 数据库
```

**设计要点：**

- Sub-I 前端**只访问自己的 8001 后端**，不直连培训系统
- 培训系统服务账号凭证保存在 `subi_knowledge_platform/.env`，**不暴露给前端**
- 两系统以 **工号（`employee_id`）** 作为人员关联主键
- 资料多版本以 **`document_group_id`** 归组，不再依赖标题完全一致

---

## 3. GitHub 仓库

| 仓库 | 地址 | 内容 |
|------|------|------|
| subi_knowledge_platform | https://github.com/0356035-cyber/subi_knowledge_platform | Sub-I 主体（BFF + Streamlit） |
| instrument-training-home | https://github.com/0356035-cyber/instrument-training-home | 仪器培训与授权系统 |
| subi-instrument-integration | https://github.com/0356035-cyber/subi-instrument-integration | 根目录统一部署配置 |

**克隆整合工作区示例：**

```bash
git clone https://github.com/0356035-cyber/subi-instrument-integration.git projects
cd projects
git clone https://github.com/0356035-cyber/subi_knowledge_platform.git
git clone https://github.com/0356035-cyber/instrument-training-home.git
```

### 3.1 `C:\projects` 目录结构（整理后）

```
C:\projects\                          ← subi-instrument-integration 仓库（Compose 编排）
├── docker-compose.yml                ← 统一部署（4 个容器，Docker Desktop 显示为 projects）
├── start-docker.bat                  ← 推荐：一键 Docker 全栈启动
├── start-local.bat                   ← 跳转本地 Python 启动（开发调试用）
├── backup-data.bat / 备份数据.bat     ← 数据库与附件备份
├── restore-data.bat / 恢复数据.bat    ← 从备份恢复
├── .env.example                      ← 环境变量模板
├── README.md
├── 项目整合交接文档.md                ← 本文档（唯一整合交接文档）
├── backups\                          ← 备份输出目录（不提交 Git）
├── instrument-training-home\         ← 独立 Git 仓库（培训系统）
│   ├── data\instrument_training.db ← 培训业务数据（不提交 Git）
│   └── streamlit_app\                ← 培训管理页面（:8501）
└── subi_knowledge_platform\          ← 独立 Git 仓库（Sub-I 资料库）
    ├── data\subi_knowledge.db        ← 资料库数据（不提交 Git）
    ├── uploads\                      ← 资料附件（不提交 Git）
    ├── frontend\                     ← Sub-I 页面（:8510，含 Dockerfile）
    └── 项目交接总结.md                ← Sub-I 功能细节文档
```

> **说明：** `instrument-training-home` 与 `subi_knowledge_platform` 是**两个独立 Git 仓库**，与整合仓库并列存放；代码用 Git 同步，业务数据用备份脚本同步。

---

## 4. instrument-training-home 侧改动

### 4.1 整合 API（对接 Sub-I）

| 文件 | 作用 |
|------|------|
| `app/schemas/integration.py` | 聚合响应 Pydantic 模型 |
| `app/services/integration_service.py` | 资质聚合业务逻辑 |
| `app/api/v1/integrations.py` | 对外整合路由 |
| `app/bootstrap.py` | Docker 启动：迁移、ID 重排、默认管理员 |

| 接口 | 说明 |
|------|------|
| `GET /api/v1/integrations/qualifications` | 按工号查单人资质（授权 + 培训 + 考核 + 汇总） |
| `GET /api/v1/integrations/qualifications/by-instrument` | 按仪器查具备资质的人员列表 |
| `GET /api/v1/users/by-employee-id/{employee_id}` | 按工号查用户 |
| `GET /health` | 健康检查（免登录） |

API 文档：http://127.0.0.1:8000/docs

### 4.2 培训 / 考核 / 授权 ID 按日期重排

**规则：** 列表 ID 与业务日期升序一致（1..N 连续编号）；在中间插入较早日期记录时，全表 ID 自动重排。

| 模块 | 排序字段 | 重排触发 |
|------|----------|----------|
| 培训记录 | `training_date` | 增删改后 + 启动时 bootstrap |
| 考核记录 | `assessment_date` | 增删改后 + 启动时 bootstrap |
| 授权记录 | `authorized_at`（授权日期） | 增删改/授权/撤销后 + 启动时 bootstrap |

**关键文件：**

| 文件 | 作用 |
|------|------|
| `app/services/record_id_resequence.py` | 通用重排逻辑，同步更新操作日志 `target_id` |
| `app/services/training_record_service.py` | 培训 CRUD 后重排，列表按日期+ID 升序 |
| `app/services/assessment_record_service.py` | 考核 CRUD 后重排，列表按日期+ID 升序 |
| `app/services/authorization_service.py` | 授权 CRUD 后重排，列表按授权日期+ID 升序 |
| `app/bootstrap.py` → `ensure_record_ids_sequenced()` | 对历史数据一次性重排 |

**测试：** `tests/test_record_id_resequence.py`

### 4.3 授权管理增强

| 功能 | 说明 |
|------|------|
| 多次授权登记 | 取消 `(user_id, instrument_id)` 唯一约束（迁移 `d4f8a1c2e6b0`） |
| 最新授权生效 | 同一人员+仪器多条记录时，**授权日期最新**的一条为当前生效授权 |
| 授权日期 | 界面显示「授权日期」；新增时可登记历史授权日期 |
| 永久有效 | 不填到期日时列表不显示到期时间 |
| 删除 | 授权列表支持删除单条记录 |
| 授权状态 | 列表标注 `is_effective`（是否当前生效） |

**关键文件：**

| 文件 | 作用 |
|------|------|
| `app/services/authorization_service.py` | `_apply_supersession()`、生效授权判定 |
| `streamlit_app/05_authorization_management.py` | 授权管理 UI |
| `streamlit_app/authorization_utils.py` | 授权状态展示工具 |
| `tests/test_authorization_supersession.py` | 最新授权覆盖旧记录 |

### 4.4 仪器管理

| 功能 | 说明 |
|------|------|
| 去掉序列号/位置 | 迁移 `e7b2c4d8f1a3`；下拉显示「名称（型号）」 |
| 重复仪器合并 | `instrument_merge_service.py`：合并培训/考核/授权记录并重排仪器 ID |
| 合并后重排 | 仪器、培训、考核、授权 ID 一并重排 |

**关键文件：** `app/services/instrument_merge_service.py`、`streamlit_app/02_instrument_management.py`

### 4.5 日期筛选

培训、考核、授权列表由日期范围改为**单日筛选**（`on_date` 参数）。

| 文件 | 作用 |
|------|------|
| `app/services/date_range.py` | 后端单日过滤 |
| `streamlit_app/date_filter.py` | Streamlit 单日选择组件 |
| `tests/test_date_range_filter.py` | 筛选逻辑测试 |

### 4.6 数据库迁移（Alembic）

| 迁移文件 | 说明 |
|----------|------|
| `d4f8a1c2e6b0_allow_multiple_authorizations_per_pair.py` | 允许多次授权 |
| `e7b2c4d8f1a3_drop_instrument_serial_and_location.py` | 删除仪器序列号、位置字段 |

Docker 启动时 `docker-entrypoint.sh` 自动执行 `alembic upgrade head` + `python -m app.bootstrap`。

### 4.7 Streamlit 管理端（:8501）

| 页面 | 文件 |
|------|------|
| 仪器管理 | `streamlit_app/02_instrument_management.py` |
| 培训管理 | `streamlit_app/03_training_management.py` |
| 考核管理 | `streamlit_app/04_assessment_management.py` |
| 授权管理 | `streamlit_app/05_authorization_management.py` |
| 操作日志 | `streamlit_app/06_operation_logs.py` |
| 统计报表 | `streamlit_app/07_statistics_report.py` |
| **Excel 导入** | `streamlit_app/08_data_import.py` |

默认管理员：工号 **3267**，密码 **123456**（首次空库自动创建）。

### 4.8 Excel 批量导入

从 `科室：培训+考核+授权.xlsx` 导入历史培训、考核、授权记录（管理员专用）。

| 项目 | 说明 |
|------|------|
| 入口 | 培训管理端 → **Excel 导入** |
| API | `POST /api/v1/imports/lab-excel?dry_run=true/false` |
| 工作表 | 默认「仪器」，可选「仪器-备份」「岗位」 |
| 预览 | `dry_run=true` 仅统计，不写库 |
| 人员 | 按姓名匹配；不存在则创建（工号 `XL0001` 起，密码 `123456`） |
| 仪器 | 按名称匹配；不存在则自动创建 |
| 去重 | 同人 + 同仪器 + 同日期 跳过 |
| 导入后 | 自动重排培训/考核/授权 ID |

**关键文件：** `app/services/excel_import_service.py`、`tests/test_excel_import_service.py`

---

## 5. subi_knowledge_platform 后端

### 5.1 整合代理（BFF）

| 文件 | 作用 |
|------|------|
| `backend/config.py` | pydantic-settings 配置（读取 `.env`） |
| `backend/integrations/instrument_training.py` | httpx 异步客户端 + JWT 缓存 |
| `backend/integrations/routes.py` | 代理路由 |
| `backend/integrations/schemas.py` | 错误/批量响应模型 |

**代理接口：**

| 接口 | 说明 | 需登录 |
|------|------|--------|
| `GET /integrations/training/health` | 培训系统连通性 | 否 |
| `GET /integrations/training/instruments` | 仪器列表（下拉选择） | 是 |
| `GET /integrations/training/qualifications` | 按工号查资质 | 是 |
| `GET /integrations/training/qualifications/by-instrument` | 按仪器查人员 | 是 |
| `POST /integrations/training/qualifications/batch-check` | 批量核验（最多 50 条） | 是 |

### 5.2 资料版本管理

| 文件 | 作用 |
|------|------|
| `backend/document_versions.py` | `document_group_id` 迁移、版本组状态刷新 |
| `backend/document_compare.py` | 两版本元数据/正文对比，Word 页码定位 |
| `backend/models.py` | `Document.document_group_id` 字段 |

**资料相关 API：**

| 接口 | 说明 |
|------|------|
| `GET /documents/{id}/versions` | 按版本组返回全部版本 |
| `GET /documents/compare?doc_id_a=&doc_id_b=` | 两版本差异对比 |
| `POST /documents/groups/{id}/refresh-version-status` | 按生效日期刷新现行/废止 |
| `POST /documents` | 支持 `document_group_id`（上传新版本） |

**版本组规则：**

- 新建资料：自动 `document_group_id = 自身 id`
- 上传新版本：继承原资料 `document_group_id`
- 现行版本：同组内 **生效日期最晚** 者为「现行」，其余为「废止」
- 启动时自动迁移旧数据（按标题回填 group_id）

### 5.3 依赖说明

`backend/requirements.txt` 关键依赖：

- `httpx` — 培训系统代理
- `bcrypt>=4.0.0,<5.0.0` — 避免 Docker 登录 500
- `python-docx` — 版本对比时提取 Word 正文与页码

### 5.4 环境变量

```env
INSTRUMENT_TRAINING_API_BASE=http://localhost:8000/api/v1
INSTRUMENT_TRAINING_SERVICE_EMPLOYEE_ID=3267
INSTRUMENT_TRAINING_SERVICE_PASSWORD=123456
INSTRUMENT_TRAINING_TIMEOUT=10
```

### 5.5 项目管理（一期，2026-06-27）

| 文件 | 作用 |
|------|------|
| `backend/models.py` | `Project` 表；`Document.project_id` 可选外键 |
| `backend/project_migrations.py` | SQLite 兼容建表 / 加列 |
| `backend/crud.py` | 项目 CRUD、`find_similar_projects` 规则匹配 |
| `frontend/project_management.py` | 项目列表 / 新建 / 详情 / 相似项目 |

**项目 API：**

| 接口 | 说明 | 权限 |
|------|------|------|
| `GET /projects/meta` | 状态、伦理类型、功效宣称建议值 | 登录 |
| `GET /projects` | 列表（关键词、状态、标签、申办方等筛选） | 登录 |
| `GET /projects/similar` | 相似项目推荐（规则版） | 登录 |
| `POST /projects` | 新建项目 | editor / admin |
| `GET /projects/{id}` | 项目详情（含资料数） | 登录 |
| `PUT /projects/{id}` | 编辑项目 | editor / admin |
| `DELETE /projects/{id}` | 删除（无关联资料时） | admin |

**资料扩展：** `GET/POST/PUT /documents` 支持 `project_id`；`general_only=true` 仅查通用资料。

**入口：** 侧边栏 → 「📁 项目管理」。

### 5.6 排期与排班（阶段 0～2 + 2026-07-03 增强）

| 文件 | 作用 |
|------|------|
| `backend/schedule_migrations.py` | 访视模板、节点、日历事件、可用性、岗位安排表；`time_slot` 迁移；`roster_week_drafts` 草稿表 |
| `backend/schedule_crud.py` | 基线日同步、默认 3 套访视模板种子、未完成周检测、草稿 CRUD |
| `backend/schedule_routes.py` | 排期/排班 API |
| `frontend/schedule_calendar.py` | 月历视图（上/下午/晚上分栏）、项目访视节点 Tab |
| `frontend/schedule_roster.py` | 周排班填报、历史周查看、Excel 导出、草稿续填、admin 部门汇总 |

**Git：** `subi_knowledge_platform` → `844110b`（`main`）

**默认访视模板：** 标准润肤 D0/D7/D14/D28、短程 D0/D3/D7、单访视 D0（首次启动自动写入）。

**周排班（2026-07-03 增强）：**

| 功能 | 说明 |
|------|------|
| 上下午分栏 | 每天分 **上午 / 下午** 填报工作安排；可选 **晚上**（勾选后显示） |
| 历史周查看 | ◀ 上周 / 下周 ▶、跳转到指定周、回到本周；已保存记录自动回填 |
| 草稿续填 | **保存草稿** 暂存进度；**保存本周工作安排** 正式入库并清除草稿；顶部提示未填完周并可一键跳转 |
| Excel 导出 | 个人周表、部门周表（格式：一周主要工作安排 + 七天 × 上/下午列） |
| 部门汇总权限 | **仅 `admin`** 可见「部门汇总」标签及导出；`editor` / `viewer` 仅「我的本周」 |
| 旧数据迁移 | 升级前按「天」填报的记录归入对应日 **上午** 栏 |

**项目日历（2026-07-03 增强）：**

| 功能 | 说明 |
|------|------|
| 时段分栏 | 选中日按上午 / 下午展示与添加事项；月历格显示「上N 下N」 |
| 晚上排班 | 默认隐藏；当天有晚上事项或点击「启用晚上排班」后显示 |
| 自动访视 | 基线同步生成的事件默认归入 **上午** |

**核心 API：**

| 接口 | 说明 |
|------|------|
| `GET /visit-templates` | 访视模板列表 |
| `GET/PUT /projects/{id}/visit-nodes` | 项目访视节点 |
| `POST /projects/{id}/visit-nodes/apply-template` | 应用模板 |
| `POST /projects/{id}/calendar/sync` | 基线日 → 自动生成回访事件 |
| `GET/POST/PUT/DELETE /calendar/events` | 日历事件（含 `time_slot`） |
| `GET /schedule/meta` | 元数据（含 `time_slots`、状态枚举） |
| `GET/PUT /availability/*` | 个人周工作安排（含 `time_slot`） |
| `PUT /availability/batch` | 批量保存一周；成功后自动清除对应周草稿 |
| `GET /availability/resume` | 未填完周列表与建议跳转周 |
| `GET/PUT/DELETE /availability/draft` | 周排班草稿读写 |
| `GET /availability/department` | 部门汇总（**仅 admin**） |
| `POST/DELETE /assignments` | 日历事件岗位安排 |

**数据表（排班相关）：**

| 表 | 用途 |
|----|------|
| `personal_availability` | 员工 + 日期 + `time_slot`（上午/下午/晚上）+ 状态 + 工作安排备注 |
| `roster_week_drafts` | 员工 + `week_start`（周一）+ 草稿 JSON |
| `calendar_events` | 日历事件 + `time_slot` |

**入口：** 侧边栏 → 「📅 项目日历」「🗓️ 周排班」；项目详情 → 「访视与排期」。

**部署提示：** 前端代码打包在 Docker 镜像内，改代码后需 `docker compose up -d --build subi-backend subi-frontend`，仅刷新浏览器不会生效。

---

## 6. Sub-I 前端（Streamlit）

### 6.1 关键文件

| 文件 | 作用 |
|------|------|
| `frontend/app.py` | 资料库主界面、版本管理、详情页 |
| `frontend/qualification_inquiry.py` | 资质查询（单人 / 批量核验） |
| `frontend/project_management.py` | 项目管理（一期） |

### 6.2 资质查询

| 功能 | 说明 |
|------|------|
| 按工号 / 按仪器查询 | 对接 BFF 代理 |
| 批量核验 | 选仪器 + 粘贴/上传工号，导出 Excel |
| 仪器下拉搜索 | 关键字筛选仪器列表 |
| 问题分项标注 | 缺培训 / 缺考核 / 考核未通过 / 未授权 / 授权过期；表格、卡片、批量核验与 Excel 导出均展示 |

**入口：** 侧边栏 → 「项目检测人员授权资质查询」

### 6.3 资料库与版本管理

| 功能 | 说明 |
|------|------|
| 独立资料详情页 | 列表/分类页「查看详情」跳转，支持返回来源页 |
| 版本时间线 | 查看历史版本详情、下载历史附件 |
| 版本对比 | 元数据差异表 + 正文左右两栏对照 |
| 页码定位 | Word 按分页符/渲染分页标记；TXT 按约 40 行/页估算 |
| 分类「全部版本」视图 | 展开显示所有历史版本记录 |
| 生效日期 | 编辑/上传新版本用 `date_input`；上传新资料可选填 |

### 6.4 版本对比使用说明

1. 进入资料详情页 → 滚至「版本对比」
2. 选择版本 A（基准）、版本 B（对照）→ 开始对比
3. 查看元数据差异表
4. 正文差异以 **左右两栏** 展示：
   - 左栏（橙色）：版本 A 旧内容 + 页码
   - 右栏（绿色）：版本 B 新内容 + 页码

> Word 页码为定位参考，取决于文档内分页符及 Word 保存时的渲染分页信息。

---

## 7. 部署与启动

### 7.1 一键启动

**Docker 全栈（家里 / 单位推荐）：**

```bat
C:\projects\start-docker.bat
```

等价命令：`cd C:\projects` → `docker compose up -d --build`

Docker Desktop 中显示为 **`projects`** 项目，内含 4 个容器（8000 / 8501 / 8001 / 8510）。

**本地 Python（仅开发调试）：**

```bat
C:\projects\subi_knowledge_platform\启动资料库系统.bat
```

或 `start-local.bat`（跳转到上述脚本）。若 Docker 已在运行，脚本会自动跳过 8000/8001/8510 的本地启动。

**特性：**

- 启动结束时健康检查 8000 / 8001 / 8510
- **Git 拉取代码后若用 Docker，务必重新运行 `start-docker.bat` 重建镜像**
- 默认登录工号 **3267**，密码 **123456**

### 7.2 访问地址

| 服务 | 地址 |
|------|------|
| Sub-I 前端 | http://localhost:8510 |
| Sub-I API 文档 | http://127.0.0.1:8001/docs |
| 培训系统 API 文档 | http://127.0.0.1:8000/docs |

### 7.3 Docker 统一部署（可选）

```bash
cd C:\projects
docker compose up -d --build
```

| 服务 | 端口 |
|------|------|
| instrument-training-home-backend | 8000 |
| instrument-training-home-frontend | 8501 |
| subi-backend | 8001 |
| subi-frontend | 8510 |

> 运行 `docker compose up -d --build` 或 `start-docker.bat` 即可启动全部服务，无需再单独启动本地 Streamlit。

**数据卷：**

| 路径 | 说明 |
|------|------|
| `instrument-training-home/data/` | 培训系统 SQLite（`instrument_training.db`） |
| `subi_knowledge_platform/data/` | Sub-I SQLite（`subi_knowledge.db`） |
| `subi_knowledge_platform/uploads/` | Sub-I 资料附件 |

**代码更新后重建培训后端：**

```powershell
cd C:\projects
docker compose up -d --build instrument-training-home-backend
```

---

## 7.4 数据库备份与恢复

一键备份脚本（复制数据库 + Sub-I 附件到带时间戳目录）：

```bat
C:\projects\备份数据.bat
```

或英文入口：`C:\projects\backup-data.bat`

| 备份内容 | 源路径 |
|----------|--------|
| 培训系统数据库 | `instrument-training-home/data/instrument_training.db` |
| Sub-I 数据库 | `subi_knowledge_platform/data/subi_knowledge.db` |
| Sub-I 附件 | `subi_knowledge_platform/uploads/` |

备份输出目录：`C:\projects\backups\yyyy-MM-dd_HHmmss\`

恢复：运行 `C:\projects\恢复数据.bat`（或 `restore-data.bat`），选择备份文件夹。

> 建议备份前停止 Docker / 本地服务（8000/8001/8510），避免 SQLite 写入冲突。若服务仍在运行，脚本会提示确认。

### 7.4.1 定时自动备份（推荐）

| 脚本 | 说明 |
|------|------|
| `install-scheduled-backup.bat` | 注册 Windows 计划任务，每天 02:00 自动备份（推荐） |
| `安装定时备份.bat` | 跳转到上述英文脚本（避免中文批处理编码问题） |
| `scheduled-backup.bat` | 静默备份入口（计划任务调用） |
| `uninstall-scheduled-backup.bat` | 移除计划任务 `ProjectsDataBackup` |
| `卸载定时备份.bat` | 跳转到上述卸载脚本 |
| `scripts/scheduled-backup.ps1` | 备份逻辑：Docker 在线备份 + 清理旧备份 |
| `scripts/sqlite_backup.py` | SQLite 在线备份辅助脚本 |

- 自动保留最近 **14** 份备份
- 日志：`backups/scheduled-backup.log`
- 首次安装：双击 `安装定时备份.bat`（建议管理员运行）

---

## 7.5 家里 / 单位换机部署指南

### 首次在新电脑搭建

1. 安装 **Docker Desktop** 并确保已启动
2. 克隆三个仓库到 `C:\projects`（见 §3 克隆示例）
3. 复制环境变量：
   ```bat
   copy C:\projects\.env.example C:\projects\subi_knowledge_platform\.env
   ```
   编辑 `.env`，填写培训系统服务账号（默认工号 3267 / 密码 123456）
4. **恢复业务数据**（从 U 盘 / 网盘 / 另一台电脑的备份）：
   ```bat
   C:\projects\恢复数据.bat
   ```
   选择 `backups\yyyy-MM-dd_HHmmss\` 备份文件夹
5. 启动全栈：
   ```bat
   C:\projects\start-docker.bat
   ```

### 日常启动（家里和单位相同）

**只需记住一个：**

```bat
C:\projects\启动系统.bat
```

（等同 `start-docker.bat`）访问 http://127.0.0.1:8510（Sub-I 资料库）、http://127.0.0.1:8501（培训管理）。

**关机停止容器（可选）：** `停止系统.bat`

### 离开当前电脑前（同步数据）

```bat
C:\projects\备份数据.bat
```

将 `C:\projects\backups\` 下最新时间戳文件夹复制到 U 盘或网盘。

### 7.6 单位离线电脑一键部署（不联网）

适用于单位电脑**无法访问 GitHub / 无法 docker pull** 的场景。在家里联网电脑打好「离线部署包」，U 盘带走后在单位双击部署。

| 脚本 | 在哪执行 | 作用 |
|------|----------|------|
| `导出离线部署包.bat` | 家里（Docker 已启动、可联网） | 打包代码 + 镜像 + 数据 |
| `deploy-offline.bat` / `离线部署.bat` | 单位（部署包目录内） | 加载镜像、恢复数据、启动全栈 |

**家里（导出）：**

```bat
C:\projects\导出离线部署包.bat
```

生成目录：`backups\offline-deploy_yyyy-MM-dd_HHmmss\`，包含：

| 内容 | 说明 |
|------|------|
| `projects\` | 三仓库 + compose 脚本快照 |
| `docker-images.tar` | 4 个已构建容器镜像（单位无需再 build） |
| `data-backup\` | 业务库与附件（可选跳过，仅更新代码时选 Y） |
| `离线部署.bat` | 单位一键入口 |

将整个 `offline-deploy_*` 文件夹复制到 U 盘。

**单位（部署，首次需安装 Docker Desktop）：**

1. U 盘拷贝 `offline-deploy_*` 到本机（任意路径均可）
2. 双击包内 **`离线部署.bat`**
3. 按提示选择目标目录（默认 `C:\projects` 或 `D:\projects`）
4. 访问 http://127.0.0.1:8510 / http://127.0.0.1:8501

> 单位电脑全程不需要 `git clone`、`git pull` 或访问外网。Docker Desktop 安装包可提前在有网电脑下载后拷过去安装。

**仅同步数据、不更新代码时：** 仍可用 `备份数据.bat` → `恢复数据.bat`，比打完整离线包更快。

### 代码更新（Git 拉取后）

```powershell
cd C:\projects
git pull

cd C:\projects\instrument-training-home
git pull

cd C:\projects\subi_knowledge_platform
git pull

cd C:\projects
docker compose up -d --build
```

### 关键原则

| 内容 | 同步方式 |
|------|----------|
| 程序代码 | Git（三个仓库分别 `git pull`） |
| 培训/授权/资料数据 | **备份脚本**（不会随 Git 自动同步） |
| Docker 镜像 | 每台电脑本地构建（`--build`） |

### 路径自动识别（C:\projects / D:\projects）

以下脚本通过 `scripts\resolve-projects-dir.bat` 自动定位工作区，**无需改盘符**：

| 脚本 | 用途 |
|------|------|
| `start-docker.bat` | Docker 全栈启动 |
| `backup-data.bat` / `备份数据.bat` | 数据备份 |
| `restore-data.bat` / `恢复数据.bat` | 数据恢复 |
| `subi_knowledge_platform\启动资料库系统.bat` | 本地 Python 启动（自动识别上级 projects 目录） |

---

## 8. 日常使用

### 资质查询

1. 登录 → 侧边栏「项目检测人员授权资质查询」
2. 单人查询或「批量核验」标签页
3. 仪器优先用下拉搜索选择

### 资料版本

1. 资料列表 / 分类详情 → 「查看详情」
2. 版本时间线查看历史版本
3. 「版本对比」左右对照核查差异
4. 上传新版本时选择生效日期（日历控件）

---

## 9. 问题排查记录

| 问题 | 原因 | 处理 |
|------|------|------|
| 登录 500（Docker） | `bcrypt 5.x` 与 `passlib` 不兼容 | 锁定 `bcrypt<5`，重建 `subi_backend` |
| 批量核验「请求参数有误」 | 同时传 `instrument_id` + `instrument_name` | 前端优先只传 `instrument_id` |
| 培训系统连接失败 | 8000 未启动或 Docker 未起 | 运行一键启动 / `start-docker.bat` |
| 版本对比无页码 | 附件非 docx/txt | 仅 docx/txt 支持正文对比；页码对 txt 为估算值 |
| 按仪器查询失败 | 8000 运行旧代码 | 重启培训后端 |
| 培训/授权 ID 与日期不一致 | 历史数据未重排或后端未重建 | `docker compose up -d --build instrument-training-home-backend` |
| 授权多条记录不知哪条生效 | — | 看列表「授权状态」列，`is_effective=true` 为当前生效 |
| 培训记录编辑报错 | `instrument_id` 为空 | 已修复；编辑时仪器字段必填 |

---

## 10. 交付清单

| 类别 | 状态 |
|------|------|
| 资质查询（单人 + 批量 + 仪器下拉） | 已完成 |
| BFF 代理端点 | 6 个 |
| 资料版本组 `document_group_id` | 已完成 |
| 版本对比 API + 左右对照 UI | 已完成 |
| 独立详情页 + 历史版本下载 | 已完成 |
| 生效日期 `date_input` | 已完成 |
| 一键启动 + Docker 脚本 | 已完成 |
| 培训/考核/授权 ID 按日期重排 | 已完成 |
| 授权多次登记 + 最新日期生效 | 已完成 |
| 仪器去序列号/位置 + 重复合并 | 已完成 |
| 单日日期筛选 | 已完成 |
| 数据库备份/恢复脚本 | 已完成 |
| 路径自动识别脚本 | 已完成 |
| Excel 批量导入 | 已完成 |
| 资质查询问题分项标注 | 已完成 |
| 离线部署包导出 / 一键部署 | 已完成 |
| 定时自动备份（计划任务） | 已完成 |
| 首次登录强制改密（两系统） | 已完成 |
| 授权到期提醒独立页 + 导出 | 已完成 |
| 操作日志日期筛选 | 已完成 |
| 统计报表 limit 提升 + 首页培训概览 | 已完成 |
| Excel 导入失败行导出 | 已完成 |
| Sub-I 全文检索（标题/标签/正文） | 已完成 |
| PDF 版本对比 | 已完成 |
| Streamlit width 参数迁移 | 已完成 |
| 仪器下拉缓存自动刷新 | 已完成 |
| 批量核验 Excel 增强（元数据 sheet） | 已完成 |

---

## 11. 后续待办（可选）

- [x] 批量核验前端
- [x] 仪器下拉搜索
- [x] `document_group_id` 版本管理
- [x] 独立资料详情页
- [x] 历史版本查看/下载
- [x] 版本对比（左右对照 + 页码定位）
- [x] 生效日期 `date_input`
- [x] 启动脚本健康检查
- [x] Sub-I Streamlit 前端容器化（:8510）
- [x] `app.py` 中其余 `use_container_width` 替换为 `width` 参数
- [x] PDF 正文对比支持
- [x] 定时自动备份
- [x] 首次登录强制改密
- [x] 授权到期提醒页
- [x] 操作日志日期筛选
- [x] 全文检索
- [x] Excel 导入失败行导出
- [x] 培训/考核/授权 ID 按日期重排
- [x] 授权多次登记与最新授权生效
- [x] 仪器管理简化（去序列号/位置）与重复合并
- [x] 培训/考核/授权单日筛选
- [x] 启动脚本 C/D 盘路径自动识别
- [x] Excel 批量导入（培训/考核/授权）
- [x] 资质查询结果分项标注（缺培训/缺考核/授权过期等）

---

## 12. 相关文档

| 文档 | 位置 |
|------|------|
| **整合交接（本文档）** | `C:\projects\项目整合交接文档.md` |
| Sub-I 资料库功能交接 | `subi_knowledge_platform/项目交接总结.md` |
| Sub-I README | `subi_knowledge_platform/README.md` |
| 培训系统 README | `instrument-training-home/README.md` |
| 极空间 + Cloudflare 部署 | `极空间部署说明.md` |
| 回家部署操作清单 | `回家部署清单.md` |
| 环境变量模板 | `.env.example` |

> 已删除 `subi_knowledge_platform/项目整合交接文档.md` 旧副本，避免与根目录文档重复。

---

## 13. 新对话快速上手

```text
请阅读 C:\projects\项目整合交接文档.md 和 subi_knowledge_platform/项目交接总结.md，
在此基础上继续维护 Sub-I 与仪器培训系统的整合功能。
后续产品规划见本文档 §14（项目知识库、权限架构 §14.12、排班与项目日历 §14.11、外用工具对接）。
Docker 一键启动：C:\projects\start-docker.bat（家里/单位相同）
访问：Sub-I http://127.0.0.1:8510 | 培训管理 http://127.0.0.1:8501
换机数据同步：备份数据.bat → 恢复数据.bat
极空间部署：回家部署清单.md
默认账号：工号 3267，密码 123456
```

---

## 14. 后续规划与演进讨论（2026-06-26 归档）

> **状态：规划中，边用边迭代** — 尚未开发，供后期调取与继续讨论。  
> **原则：** 系统负责「找得到、挂得住、追得回」；Word / Excel / SPSS / AI 负责「算得准、写得顺」；定稿与结果回系统归档。

### 14.1 业务背景与诉求

当前 Sub-I 资料库主要服务**项目负责人日常工作**：法规、伦理要求、内部规定、通用模版等。

后续希望扩展为**按项目维度的知识库**，解决项目增多后的回溯与复用问题：

| 痛点 | 说明 |
|------|------|
| 项目差异大 | 功效不同（保湿 / 修护等）、申办方不同、伦理要求不同 |
| 资料分散 | 方案、伦理批件、SAP、统计报告、总结报告等难统一检索 |
| 重复劳动 | 研究方案、统计分析报告、研究报告需反复撰写 |
| 统计重复 | 项目统计较简单，希望固定套路能一键出表出图 |

### 14.2 与现有系统的关系

**现有能力（可直接扩展）：**

- `Document` 模型：分类、**标签**、摘要、版本、`content_text` 全文检索、附件上传下载
- 资料版本组 `document_group_id`、版本对比
- 用户权限 admin / editor / viewer
- 与培训系统资质查询联动（BFF 代理）

**尚缺能力（规划方向）：**

- **项目实体**（Project）：台账、多标签、状态、申办方、功效宣称等元数据
- 资料与项目关联（`Document.project_id`，通用资料可仍不挂项目）
- **排班与项目日历**：周排班、项目访视节点、人员岗位安排（见 §14.11）
- 相似项目推荐（先规则匹配，后期可选语义检索）
- 模版导出 / 定稿归档工作流
- 简单统计一键分析（固定 Excel 模版 + 固定分析类型）

### 14.3 架构决策：并入现有系统，不另建子库

**结论：扩展 `subi_knowledge_platform`，新增「项目管理」模块；不另建第三套 Docker / 平行子系统。**

| 方案 | 结论 | 理由 |
|------|------|------|
| **A. 并入 Sub-I 资料库** | ✅ **采用** | 同一入口、同一账号；资料引擎可复用；部署栈不变（仍 4 容器） |
| B. 新建子库（第三套应用） | ❌ 不采用 | 双登录、数据重复、运维成本翻倍 |
| C. 完全独立系统 | ❌ 不采用 | 当前规模过重 |

**模块划分（逻辑分区，物理一家）：**

```
Sub-I 前端 (:8510)
├── 通用资料库      ← 法规 / 模版 / 内部要求（现有）
├── 项目管理        ← 项目台账 / 标签检索 / 项目资料归档（规划）
│   └── 排班与日历  ← 周排班 / 项目日历 / 访视节点同步（规划，见 §14.11）
└── 资质查询        ← 调 instrument-training-home API（现有；排班选人时可联动）

instrument-training-home (:8501)  ← 培训 / 考核 / 授权，保持独立
```

**何时才考虑拆出子库：** 使用人群完全分离（如外部 CRO 独立权限）、数据须物理隔离、或功能膨胀到 ERP 级项目管理。

**权限与可见性（2026-06-27 团队确认）：** 采用**总库合并、按角色与各看各的**，不每人一套子库；细则见 **§14.12**。

### 14.4 功能可行性评估

| 设想 | 可行性 | 优先级 | 说明 |
|------|--------|--------|------|
| 项目知识库 + 多标签检索 | ✅ 高 | **P0** | 性价比最高；先统一标签规范 |
| 相似项目查找（规则版） | ✅ 中高 | P1 | 同功效 + 同申办方 + 同伦理类型等 |
| 全文 / 跨项目检索 | ✅ 中 | P1～P2 | 可扩展现有 `content_text`；量大后考虑 Whoosh |
| 方案 / 报告模版初稿 | ✅ 中 | P1～P2 | 模版填空 + 参考旧项目；须人工审核 |
| AI 章节辅助撰写 | ⚠️ 中 | P2～P3 | 幻觉与合规风险；仅作初稿辅助 |
| 简单统计一键出结果 | ✅ 中高 | P1～P2 | 先固定 2～3 种分析套路 |
| 复杂统计平台 | ❌ 低 | 不做 | 继续 SPSS / R，结果回系统归档 |
| 语义相似项目推荐 | ⚠️ 中 | P3 | 需向量检索，后期按需 |
| 普通月历 + 项目访视节点自动同步 | ✅ 高 | **P1** | 先做日历 MVP；挂项目时按基线日生成回访事件（§14.11） |
| 每周人员排班 + 部门汇总 | ✅ 高 | P1～P2 | 员工填可用性；指定负责人看全员；普通人只看自己 |
| 项目日人员岗位 + 可排班查询 | ✅ 中高 | P2 | 日历节点挂人；结合可用性与资质查询推荐人选 |
| 冲突预警 / 负荷分析 / 审批锁定 | ✅ 中 | P3 | 边用边迭代；非首期 |
| 拖拽式甘特图 | ⚠️ 低 | **不做（现阶段）** | 交互重、开发成本高；部门规模下普通日历够用 |

### 14.5 自建 vs 外用：判断标准

每出现新需求，按三条快速决策：

| 问题 | 倾向自建（进 Sub-I） | 倾向外用 |
|------|---------------------|----------|
| 是否与项目元数据 / 权限强绑定？ | 是 | 否 |
| 是否需合规留痕（版本、上传人、定稿）？ | 是 | 协作可外用，定稿回系统 |
| 是否有成熟专业工具？ | 无或很弱 | Word、SPSS、GraphPad、大模型等 |

**推荐分工：**

| 能力 | 归属 |
|------|------|
| 项目信息、标签、检索、定稿归档、操作日志 | **自研 Sub-I** |
| Word 排版、修订、批注 | **Microsoft Word** |
| 数据录入 / 清洗 | **Excel（标准模版）** |
| 统计计算（复杂或新型分析） | **SPSS / GraphPad / R** |
| AI 润色某一章节（可选） | **大模型 API** |
| 培训 / 考核 / 授权 | **instrument-training-home（现有）** |
| 周排班、项目日历、访视节点、人员岗位留痕 | **自研 Sub-I（§14.11）** |
| 复杂甘特 / MS Project 级资源均衡 | **外用**（结果或截图回系统归档） |

### 14.6 外用软件对接模式（最大化效率）

核心原则：**一个项目、一条链路、一个归档入口**。

#### 模式 1：模版驱动（方案 / 报告）

```
Sub-I：选项目 → 选相似旧项目 → 导出项目信息 + 下载 Word 模版
  ↓
Word：打开模版（字段已填）→ 人工改专业内容 → 修订模式
  ↓
Sub-I：上传定稿 → 挂到该项目 → 打标签（如「研究方案 V2」）
```

#### 模式 2：数据 → 统计 → 报告

```
Sub-I：下载标准 Excel 数据模版
  ↓
Excel：录入 / 清洗数据
  ↓
SPSS / GraphPad：按 SAP 跑分析（或后期系统内固定分析）
  ↓
导出：结果表 + 图（命名规范：项目编号_终点_分析类型_日期）
  ↓
Sub-I：上传到该项目「统计分析」分类；可选填入报告模版固定章节
```

#### 模式 3：AI 辅助（可选，后期）

```
Sub-I：导出「项目摘要包」（基本信息 + 相似项目目录 + 章节摘录，脱敏）
  ↓
大模型：仅生成某一节初稿（如研究背景、样本量依据）
  ↓
Word：粘贴 → 人工逐句修改 → 定稿回 Sub-I
```

> AI 不直连数据库；系统记录参考了哪些项目、哪次辅助（留痕）。

#### 模式 4：知识检索（近期即可，不必等 AI）

- 统一标签体系（功效、部位、申办方、是否伦理、仪器类型等，先定 10～20 个）
- 每项目至少归档：方案、伦理批件、SAP、统计报告、总结报告
- 组合筛选即可解决大部分「找类似项目」需求

### 14.7 建议实施分期

| 阶段 | 系统内 | 外用 | 讨论重点 |
|------|--------|------|----------|
| **一期** | 项目表 + 标签 + 资料挂项目 + 组合筛选 | Word 模版手动维护 | 标签规范、资料分类 |
| **一期后半 / 二期初** | 普通月历 + 访视节点自动同步（§14.11 阶段 0～1） | — | 访视模板套路、日历权限 |
| **二期** | 周排班 + 日历挂人 + 模版下载、统计结果归档 | SPSS + 固定 Excel | 哪 2～3 种分析可内化 |
| **三期** | 简单统计一键、规则版相似项目推荐 | 复杂统计仍 SPSS | 合规与脱敏 |
| **四期（按需）** | AI 章节辅助接口、语义检索 | — | 医院信息科确认外网 / AI 政策 |

**一期技术要点（预留，不必一次做完）：**

1. 新增 `Project` 表 + 前端菜单「项目管理」
2. `Document` 增加可选 `project_id`（`NULL` 表示仍属通用库）
3. 列表支持「仅通用 / 仅某项目 / 标签组合」筛选
4. 仪表盘增加「进行中项目」「最近更新项目资料」

### 14.8 迭代讨论机制

**工作方式：** 后期一边真实项目使用、一边讨论哪些进系统、哪些接外用。

每完成一个真实项目，建议花 15 分钟回顾：

1. 哪些步骤重复最多？（优先模版化 / 自动化）
2. 哪份文档最费时间？（优先模版或「参考旧项目」）
3. 哪种统计反复出现？（优先「一键分析」候选）
4. 哪些必须留痕？（必须在 Sub-I 归档）
5. 哪些 Word / SPSS 流程已经好用？（保持外用，只接归档）
6. 日历 / 排班是否仍要回 Excel 补？（优先做进 §14.11 下一期）
7. 访视模板是否要新增一种？（更新模板库）

**动手前建议团队对齐：**

- 标签体系由谁定、有哪些标准值
- 统计分析有几种「标准套路」（列 3～5 个真实 SAP / 报告对照）
- 临床资料外网访问与 AI 使用是否符合医院 / 申办方合规要求
- 部门常用访视模板有几种（D 天组合）、谁有权限看全员排班（见 §14.11）

### 14.9 规划项状态追踪

| 规划项 | 状态 | 备注 |
|--------|------|------|
| 项目管理模块 | **一期已上线** | 台账 CRUD、列表筛选、相似项目（规则版） |
| 资料挂项目 `project_id` | **一期已上线** | 上传/编辑可选项目；资料列表按范围筛选 |
| 标签体系规范文档 | 未开始 | 使用前需团队共识 |
| Word 模版导出 | 未开始 | 二期 |
| 统计 Excel 模版 + 结果归档 | 未开始 | 二期 |
| 简单统计一键分析 | 未开始 | 二期，先摸底标准套路 |
| AI 章节辅助 | 未开始 | 三期可选 |
| 语义相似项目 | 未开始 | 四期可选 |
| 排班与项目日历模块 | **阶段 0～2 已上线** | 访视节点、月历、基线同步、周排班 |
| 访视节点模板库 | **已上线（3 套默认模板）** | 启动时自动种子；可继续增补 |
| 周排班上下午分栏 + 晚上可选 | **2026-07-03 已上线** | 周排班与项目日历均已分栏 |
| 周排班 Excel 导出（个人/部门） | **2026-07-03 已上线** | 部门导出仅 admin |
| 周排班草稿续填 + 历史周查看 | **2026-07-03 已上线** | 保存草稿 / 未填完周跳转 |
| 周排班部门汇总 | **已上线（仅 admin）** | editor/viewer 不显示该标签 |
| 日历冲突预警 + 资质联动 | 未开始 | §14.11 阶段 3 余下项 |
| 复制上周排班 | 未开始 | §14.11 阶段 3 |
| 总库分层权限（§14.12） | 未开始 | 项目成员可读、负责人可编；模版仅负责人 |
| 部门内部要求可见标识 | 未开始 | 科室主任标「全员须知」/「项目负责人须知」 |
| 系统维护员角色 `system_maintainer` | 未开始 | 用户管理 + 运维；兼负责人时仅见/管自己的项目 |
| 主任修改提醒（§14.12.4） | 未开始 | 负责人登录弹窗 / 重点标识 |

### 14.10 新对话延续本规划

```text
请阅读 C:\projects\项目整合交接文档.md §14（尤其 §14.12 权限、§14.11 排班日历），
我们继续讨论 Sub-I 项目管理模块 / 权限实现 / 排班日历 / 某期功能开发。
当前架构决策：总库合并、不每人子库；扩展 subi_knowledge_platform，仍 4 容器；
培训授权系统保持 instrument-training-home 独立。
```

### 14.11 排班与项目日历（2026-06-27 归档，2026-07-03 迭代记录）

> **状态：阶段 0～2 已上线；2026-07-03 完成上下午分栏、Excel 导出、草稿续填**（实现细节见 §5.6，提交 `844110b`）。  
> **策略：先用普通日历跑通，再逐步丰富内容**；不做拖拽式甘特图（现阶段）。  
> **架构：并入 `subi_knowledge_platform`「项目管理」子模块，不另建独立系统。**

#### 14.11.1 业务诉求

| 模块 | 说明 |
|------|------|
| **每周人员排班** | 每个员工可提交/编辑某一周的可用性；**指定负责人**可查看部门全员排班汇总；**普通人员**仅查看自己的排班 |
| **项目安排（日历）** | 在日历中展示项目排期；某天可查看该项目有哪些人参与、分别担任什么岗位；支持部门级项目与人员安排汇总 |
| **可排班查询** | 项目负责人可查询：哪些日期适合排班、哪些人员在该日期可用 |
| **访视节点自动进日历** | 将项目加入日历时，按项目已维护的回访/访视节点**自动生成**日历事件，避免每次手动逐条录入 |

与现有**资质查询**的关系：排「仪器操作」等岗位时，可调用 `instrument-training-home` API 校验授权是否有效（提示为主，不硬拦截）。资质查询页已有「适合项目排班前人员核对」的定位，与本模块天然衔接。

#### 14.11.2 架构决策（与 §14.3 一致）

| 方案 | 结论 | 理由 |
|------|------|------|
| 并入 Sub-I 资料库 | ✅ **采用** | 同一账号、同一项目台账；部署栈不变（仍 4 容器） |
| 并入 instrument-training-home | ❌ 不采用 | 该系统职责是培训/考核/授权，非项目运营调度 |
| 另建第三套独立系统 | ❌ 不采用 | 双登录、人员与项目数据重复、极空间运维成本翻倍 |

#### 14.11.3 UI 决策：普通日历优先，暂不做甘特图

**普通日历**：按月查看每天有哪些事项（列表或简单格子），点某天看详情；开发快、符合「先看哪天有什么事」的习惯。

**拖拽式甘特图**（现阶段不做）：用横向色块表示任务时间跨度，鼠标拖动色块左右调整起止日期；适合任务极多、前后依赖复杂的 PM 场景。部门当前规模下性价比低，后期若项目并行暴增再评估。

#### 14.11.4 访视节点自动同步日历（核心设计）

**两层数据：**

1. **项目主数据（录入一次）**  
   - 访视/回访节点表：节点代号（如 V0/V1）、名称、相对基线天数（如 +7）、备注  
   - 可选绑定**访视模板**（部门常用 D 天组合，如 D0/D7/D14/D28）

2. **日历事件（按基线日批量生成）**  
   - 将项目「挂到日历」时，选择**基线日期**（首例入组日、项目启动日或伦理批件日等）  
   - 系统按节点规则自动生成事件，例如：`3月17日 — XX项目 · V2 回访`  
   - 支持手动补充一次性事件（伦理递交、启动会等）

**两种模式（可并存）：**

| 模式 | 适用 | 操作 |
|------|------|------|
| 模板驱动 | 同类型项目访视节奏固定 | 选标准访视模板 → 填基线日 → 一键生成 |
| 项目自定义 | 方案访视节奏各异 | 在项目内维护节点表 → 挂日历时选基线日 → 同步 |

**生成后仍可调整（非锁死）：**

- 修改基线日 → 提示「是否按新日期重算所有回访？」  
- 单个节点改期（受试者延期等）→ 仅改该条事件  
- 增删节点 → 支持「重新同步」该项目日历

**概念数据表（开发预留）：**

| 表 / 实体 | 用途 |
|-----------|------|
| `VisitTemplate` / `VisitTemplateNode` | 部门标准访视模板及节点 |
| `ProjectVisitNode` | 某项目具体访视节点（可来自模板复制） |
| `CalendarEvent` | 日历事件：日期、`time_slot`、项目、节点类型、标题、是否自动生成 |
| `ScheduleWeek` | 某年某周排班周期（草稿 / 已发布 / 已锁定）— **规划预留，未建表** |
| `PersonalAvailability` | 员工 + 日期 + `time_slot` + 状态 + 工作安排备注 |
| `RosterWeekDraft` | 员工 + 周一起始日 + 草稿 JSON — **2026-07-03 已上线** |
| `ProjectAssignment` | 项目 + 日期 + 员工 + 岗位/角色 |

#### 14.11.5 权限扩展（与 §14.12 统一）

排班与日历的可见/可改范围遵循 **§14.12 总库权限**；本模块额外约定：

| 能力 | 规则 |
|------|------|
| 全员周排班汇总 | 规划：`schedule_manager`（科室主任）及 `admin`；**当前实现：仅 `admin`**（2026-07-03 确认） |
| 项目日历与人员安排 | 项目成员可读；项目负责人可编辑；主任可全盘修改（改后触发 §14.12.4 提醒） |

> 开发前需团队对齐：**谁担任 `schedule_manager`**（建议对应科室主任）。若科室主任需看部门汇总，可暂授 `admin` 或后续增加 `schedule_manager` 角色。

#### 14.11.6 分阶段实施（边用边完善）

| 阶段 | 内容 | 验收标准 |
|------|------|----------|
| **0** | 项目台账 + 访视节点表 + 2～3 个访视模板 | 能建真实项目并录好节点 |
| **1 — 日历 MVP** | 月历视图；项目挂日历 + 基线日自动生成回访；手动补一次性事件；按权限查看 | 1～2 个真实项目试跑一个月，月历比 Excel 省事 |
| **2 — 人员排班** | 员工填周可用性；负责人在节点上挂人+岗位；全员汇总 vs 仅看自己 | 能回答「下周三 V2 回访谁有空」 |
| **3 — 省心防错** | 同人同日多项目冲突标红；仪器岗资质提示；复制上周排班；导出周表 Excel；改基线日重算 | 减少人工核对与遗漏；**导出周表 Excel 已于 2026-07-03 上线** |
| **4 — 按需** | 审批锁定、负荷热力图、人力缺口推荐、日历链到项目资料 | 按真实痛点择优 |

**阶段 1 暂不做的：** 拖拽甘特图、冲突预警、审批流。（周排班填报、Excel 导出已分别于 2026-06-27、2026-07-03 上线。）

#### 14.11.7 后期可扩展能力（非首期）

按投入产出比排序，供迭代时选取：

1. 冲突与超负荷预警（同人同日多项目、周工时阈值）  
2. 排班与资质联动（无授权警告）  
3. 周排班模板 / 复制上周  
4. 部门负荷热力图  
5. 项目人力缺口视图 + 候选人推荐  
6. 导出 Excel / 打印周表 — **周排班 Excel 已上线（2026-07-03）**；打印版式待做
7. 排班变更留痕（谁改了谁的排班）  
8. 日历事件关联项目资料（如伦理批件链接）  
9. 请假 / 出差登记与排班一体  
10. 关键岗位替补提示、里程碑提醒、历史人力投入统计  

**不建议自建：** 复杂甘特 / MS Project 级资源均衡、与医院 HR 考勤系统对接（除非有明确 API）。

#### 14.11.8 开发前待团队准备

1. **列 2～3 套常用访视模板**（真实项目中的 D 天组合）  
2. **明确日历/排班权限**：谁看全员、谁只管自己项目、谁可锁定周排班  
3. **定基线日惯例**：默认用首例入组日、启动日还是其他字段  

#### 14.11.9 新对话延续排班规划

```text
请阅读 C:\projects\项目整合交接文档.md §14.11，
我们继续实现 Sub-I 排班与项目日历（阶段 0/1 表结构、页面、权限）。
策略：普通月历先行，项目挂日历时按访视节点自动生成事件，边用边迭代。
仓库：subi_knowledge_platform；培训系统仅提供资质查询联动。
```

### 14.12 总库权限与可见性（2026-06-27 团队确认）

> **状态：规划已确认，尚未开发。**  
> **结论：不每人一套子库；一个 Sub-I 总库，按角色与项目关系控制可见与可改。**

#### 14.12.1 架构原则

| 原则 | 说明 |
|------|------|
| **总库合并** | 通用资料、各项目资料、排班日历在同一系统；用 `project_id`、分类、角色区分，不物理拆库 |
| **各看各的** | 普通成员默认只见与自己相关的项目内容 + 规定可见的通用资料 |
| **主任全盘** | 临床研究部主任、科室主任可查阅并修改全部项目与资料（含排班） |
| **不建个人子库** | 人员调动、换人负责时只改项目归属/成员关系，不迁库 |

#### 14.12.2 角色与系统账号映射（建议）

| 业务角色 | 系统角色（建议） | 可见范围 | 可改范围 |
|----------|------------------|----------|----------|
| 普通成员（非本项目） | `viewer` | 法规、**全员须知**类内部要求；不参与的项目不可见 | 仅自己的周排班（若有） |
| **项目成员** | `viewer` + 项目成员关系 | **仅该项目**相关资料、日历、排班 + 法规 + **全员须知** | 不可改项目资料与日历（只读） |
| **项目负责人** | `editor` + 项目负责人关系 | 自己负责的项目 + 法规 + **通用模版** + **全员须知** + **项目负责人须知** | **可编辑**所负责项目的资料、日历、排班 |
| **科室主任** | `schedule_manager`（或等价角色） | **本科室全部项目** + 全部通用资料 + 全员排班汇总 | **可修改全部业务内容**（含他人项目）；可设定内部要求可见标识（§14.12.3） |
| **临床研究部主任** | `admin` | **全部业务内容** | **可修改全部** + 用户与角色管理（业务侧） |
| **系统维护员** | `system_maintainer` + **项目负责人关系**（常见叠加） | **用户管理、系统运维**；业务上**仅**自己负责/参与的项目 + 法规 + 全员须知 +（若为负责人）模版与负责人须知；**不可**查阅他人项目或部门全盘 | 用户账号与角色配置、系统参数与运维；**可编辑自己负责的项目**（与负责人相同）；**不可**因维护员身份修改他人项目 |

**系统维护员（2026-06-27 确认，第九轮修正）：**

- 维护员**通常也是项目负责人之一**，权限为 **运维能力 + 项目负责人能力** 叠加，而非「完全脱离业务」。  
- **运维侧：** 用户管理、系统状态/配置、备份与升级等；**无**科室主任 / 部主任级别的全盘业务视野。  
- **业务侧（与负责人一致，仅限自己的项目）：** 可见并管理 `lead_employee_id` 为自己工号的项目及其资料、日历、排班；可作为成员只读参与其他项目（若有 `ProjectMember` 关系）。  
- **通用资料：** 与项目负责人相同 — 法规、**全员须知**全员可见；若本人为至少一个项目的负责人，则可见**通用模版**与**项目负责人须知**。  
- **明确禁止：** 仅凭 `system_maintainer` **不能**浏览或修改**非本人负责且非本人参与**的项目；不能替代科室主任查看本科室全部项目（除非另被授予 `schedule_manager` / `admin`）。  
- 与 `admin`（临床研究部主任）区分：`admin` = 业务全盘 + 用户管理；`system_maintainer` = 技术运维 + 用户管理 + **仅自己的项目（负责人身份）**。

#### 14.12.3 通用资料库分层可见（团队确认）

现有资料按**分类**划分可见性；项目成员**不**因参与项目而自动获得全部通用库权限。

| 资料类型 | 典型分类 | 谁可以看 | 谁可以改 |
|----------|----------|----------|----------|
| **法规 / 指南 / 伦理要求** | 法规、伦理等 | **全员**（含项目成员） | 项目负责人 / 主任 / `admin` |
| **通用模版** | 方案模版、报告模版等 | **仅项目负责人**及以上（`editor` 且负责至少一项目，或主任 / `admin`） | 同上 |
| **部门内部要求** | 内部规定 | **由科室主任设定可见标识**（见下表） | 科室主任 / `admin` 发布与打标；维护员若兼负责人则按标识**阅读**，不因其运维身份编辑他人发布的内部要求 |
| **项目专属资料** | 挂 `project_id` 的 Document | **该项目成员**可读；非成员不可见 | **项目负责人**可编辑；主任 / `admin` 可编辑 |

**部门内部要求 — 可见范围标识（2026-06-27 确认）：**

科室主任（`schedule_manager`）或临床研究部主任（`admin`）在发布/编辑内部要求时，须选择**可见范围标识**（建议字段 `audience_scope`）：

| 标识 | 含义 | 谁可以看 |
|------|------|----------|
| `all_staff`（**全员须知**） | 部门人人都要知道的规定 | **全员**（含普通成员、项目成员） |
| `project_leads_only`（**项目负责人须知**） | 仅负责人需要掌握的要求 | **项目负责人**及以上（主任 / `admin`）；项目成员**不可见** |

- 分类仍归「部门内部要求」大类；**可见性由标识决定**，不必再拆物理子库。  
- 列表与检索页对项目成员**过滤掉** `project_leads_only` 条目；负责人仪表盘可分区展示「全员须知 / 负责人须知」。  
- **历史数据：** 内部要求**尚未开始录入具体内容**；待框架搭好后再统一部署，**发布时必选** `audience_scope`，无历史迁移负担。

要点（2026-06-27 确认）：

1. **项目成员可读、负责人可编辑** — 项目内资料与日历/排班均按此执行。  
2. **项目成员只看与项目相关的内容** — 不参与的项目、无权限的通用模版、**项目负责人须知**均不可见。  
3. **法规类全员可看；模版类仅对项目负责人开放** — 普通项目成员不能浏览通用模版库。  
4. **内部要求由科室主任打标识** — 「全员须知」vs「项目负责人须知」，做好分类标识即可。

#### 14.12.4 主任修改 → 负责人显著提醒（团队确认）

科室主任、临床研究部主任对**非本人负责项目**的内容进行修改后，**项目负责人**须在下次登录时收到明显提示，便于及时处理。

**适用对象（至少包括）：**

- 项目资料：上传、编辑、删除、新版本、元数据变更  
- 项目日历 / 访视节点 / 排班与人员岗位  
- 项目负责人字段或项目成员名单变更  

**提醒方式（可组合，首期至少实现一种）：**

| 方式 | 说明 |
|------|------|
| **登录弹窗** | 负责人登录后优先展示「待处理变更」列表：哪个项目、什么内容、谁改的、何时 |
| **全局角标** | 导航栏「我的项目」或铃铛图标显示未读条数 |
| **列表重点标识** | 项目列表 / 资料列表中，被主任改过的条目高亮（如色条、「主任已修改」标签） |
| **详情页提示** | 打开该资料或日历时，顶部 `st.warning` / 横幅说明变更摘要 |

**技术预留：**

- 操作审计表 `AuditLog`（或扩展现有日志）：`actor_id`、`project_id`、`entity_type`、`entity_id`、`action`、`summary`、`created_at`  
- 负责人已读状态：`ProjectLeadNotification`（`user_id`、`audit_id`、`read_at`）；未读则持续提醒直至确认或标记已读  
- 主任自己的修改可不通知自己；负责人修改自己项目不弹「主任修改」类提醒  

**交互原则：**

- 提醒信息须含：**修改人姓名/角色、时间、项目、变更摘要、跳转链接**  
- 支持「我知道了」批量已读；重要变更可保留详情页高亮直至负责人打开过该条目  

#### 14.12.5 与一期项目模块的衔接

实现项目管理（§14.7 一期）时同步落地：

1. `Project` 表：`lead_employee_id`（负责人）、项目成员多对多表 `ProjectMember`  
2. `Document`：`project_id` 可空；`category` + `audience_scope`（`all_staff` / `project_leads_only` / 模版与法规专用值）驱动 §14.12.3 可见性  
3. `User.role` 扩展：`system_maintainer`；业务 API 按 **项目关系**（`lead_employee_id` / `ProjectMember`）过滤 — 维护员**无**主任级全盘接口，但**有**自己负责项目的完整读写  
4. 登录后接口：`GET /notifications/pending-changes` — 凡担任项目负责人的账号均调用（含兼维护员者，针对**其负责项目**被主任修改的提醒）
5. 写操作中间件：若 `actor` 为科室主任 / `admin` 且非该项目负责人，写入 `AuditLog` 并生成负责人待办提醒

#### 14.12.6 新对话延续权限规划

```text
请阅读 C:\projects\项目整合交接文档.md §14.12，
实现 Sub-I 总库权限：项目成员只读、负责人可编；法规全员可读、模版仅负责人；
内部要求由科室主任标 audience_scope（全员须知 / 项目负责人须知）；
主任改项目内容后负责人弹窗/高亮；system_maintainer 兼负责人时可管自己的项目，不见他人项目全盘。
```