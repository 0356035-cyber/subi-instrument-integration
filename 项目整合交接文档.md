# Sub-I 与仪器培训系统整合 — 项目交接文档

> 更新日期：2026-06-25（第四轮：体验优化与功能补全）
> 交接范围：`subi_knowledge_platform` + `instrument-training-home` API 整合与培训系统日常管理功能

---

## 1. 整合目标与成果

将 **Sub-I 临床研究资料库** 与 **仪器培训授权系统（instrument-training-home）** 打通，并持续完善资料库版本管理能力。

| 模块 | 状态 |
|------|------|
| 资质查询（按工号 / 按仪器 / 批量核验） | **已上线可用** |
| 仪器下拉搜索 + BFF 代理 | **已上线可用** |
| 资料版本组 `document_group_id` | **已上线可用** |
| 独立资料详情页 + 历史版本查看/下载 | **已上线可用** |
| 版本对比（元数据 + 正文左右对照 + 页码定位） | **已上线可用** |
| 生效日期 `date_input` 统一校验 | **已上线可用** |
| Sub-I Streamlit 前端容器化（:8510） | **已上线可用** |
| 培训/考核/授权 ID 按日期重排 | **已上线可用** |
| 授权多次登记 + 最新授权日期生效 | **已上线可用** |
| 仪器管理去序列号/位置 + 重复仪器合并 | **已上线可用** |
| 培训/考核/授权单日筛选 | **已上线可用** |

---

## 2. 整体架构

```
用户浏览器 (Streamlit :8510)
        ↓ Bearer Token（Sub-I 用户登录态）
Sub-I 后端 (FastAPI :8001)          ← BFF 代理层，隐藏培训系统服务账号
        ↓ 服务账号 JWT
instrument-training-home (FastAPI :8000)
        ↓
SQLite 数据库
```

**设计要点：**

- Sub-I 前端**只访问自己的 8001 后端**，不直连培训系统
- 培训系统服务账号凭证保存在 `subi_knowledge_platform/.env`，**不暴露给前端**
- 两系统以 **工号（`employee_id`）** 作为人员关联主键
- 资料多版本以 **`document_group_id`** 归组，不再依赖标题完全一致

---

## 3. GitHub 仓库

| 仓库 | 地址 | 内容 |
|------|------|------|
| subi_knowledge_platform | https://github.com/0356035-cyber/subi_knowledge_platform | Sub-I 主体（BFF + Streamlit） |
| instrument-training-home | https://github.com/0356035-cyber/instrument-training-home | 仪器培训与授权系统 |
| subi-instrument-integration | https://github.com/0356035-cyber/subi-instrument-integration | 根目录统一部署配置 |

**克隆整合工作区示例：**

```bash
git clone https://github.com/0356035-cyber/subi-instrument-integration.git projects
cd projects
git clone https://github.com/0356035-cyber/subi_knowledge_platform.git
git clone https://github.com/0356035-cyber/instrument-training-home.git
```

### 3.1 `C:\projects` 目录结构（整理后）

```
C:\projects\                          ← subi-instrument-integration 仓库（Compose 编排）
├── docker-compose.yml                ← 统一部署（4 个容器，Docker Desktop 显示为 projects）
├── start-docker.bat                  ← 推荐：一键 Docker 全栈启动
├── start-local.bat                   ← 跳转本地 Python 启动（开发调试用）
├── backup-data.bat / 备份数据.bat     ← 数据库与附件备份
├── restore-data.bat / 恢复数据.bat    ← 从备份恢复
├── .env.example                      ← 环境变量模板
├── README.md
├── 项目整合交接文档.md                ← 本文档（唯一整合交接文档）
├── backups\                          ← 备份输出目录（不提交 Git）
├── instrument-training-home\         ← 独立 Git 仓库（培训系统）
│   ├── data\instrument_training.db ← 培训业务数据（不提交 Git）
│   └── streamlit_app\                ← 培训管理页面（:8501）
└── subi_knowledge_platform\          ← 独立 Git 仓库（Sub-I 资料库）
    ├── data\subi_knowledge.db        ← 资料库数据（不提交 Git）
    ├── uploads\                      ← 资料附件（不提交 Git）
    ├── frontend\                     ← Sub-I 页面（:8510，含 Dockerfile）
    └── 项目交接总结.md                ← Sub-I 功能细节文档
```

> **说明：** `instrument-training-home` 与 `subi_knowledge_platform` 是**两个独立 Git 仓库**，与整合仓库并列存放；代码用 Git 同步，业务数据用备份脚本同步。

---

## 4. instrument-training-home 侧改动

### 4.1 整合 API（对接 Sub-I）

| 文件 | 作用 |
|------|------|
| `app/schemas/integration.py` | 聚合响应 Pydantic 模型 |
| `app/services/integration_service.py` | 资质聚合业务逻辑 |
| `app/api/v1/integrations.py` | 对外整合路由 |
| `app/bootstrap.py` | Docker 启动：迁移、ID 重排、默认管理员 |

| 接口 | 说明 |
|------|------|
| `GET /api/v1/integrations/qualifications` | 按工号查单人资质（授权 + 培训 + 考核 + 汇总） |
| `GET /api/v1/integrations/qualifications/by-instrument` | 按仪器查具备资质的人员列表 |
| `GET /api/v1/users/by-employee-id/{employee_id}` | 按工号查用户 |
| `GET /health` | 健康检查（免登录） |

API 文档：http://127.0.0.1:8000/docs

### 4.2 培训 / 考核 / 授权 ID 按日期重排

**规则：** 列表 ID 与业务日期升序一致（1..N 连续编号）；在中间插入较早日期记录时，全表 ID 自动重排。

| 模块 | 排序字段 | 重排触发 |
|------|----------|----------|
| 培训记录 | `training_date` | 增删改后 + 启动时 bootstrap |
| 考核记录 | `assessment_date` | 增删改后 + 启动时 bootstrap |
| 授权记录 | `authorized_at`（授权日期） | 增删改/授权/撤销后 + 启动时 bootstrap |

**关键文件：**

| 文件 | 作用 |
|------|------|
| `app/services/record_id_resequence.py` | 通用重排逻辑，同步更新操作日志 `target_id` |
| `app/services/training_record_service.py` | 培训 CRUD 后重排，列表按日期+ID 升序 |
| `app/services/assessment_record_service.py` | 考核 CRUD 后重排，列表按日期+ID 升序 |
| `app/services/authorization_service.py` | 授权 CRUD 后重排，列表按授权日期+ID 升序 |
| `app/bootstrap.py` → `ensure_record_ids_sequenced()` | 对历史数据一次性重排 |

**测试：** `tests/test_record_id_resequence.py`

### 4.3 授权管理增强

| 功能 | 说明 |
|------|------|
| 多次授权登记 | 取消 `(user_id, instrument_id)` 唯一约束（迁移 `d4f8a1c2e6b0`） |
| 最新授权生效 | 同一人员+仪器多条记录时，**授权日期最新**的一条为当前生效授权 |
| 授权日期 | 界面显示「授权日期」；新增时可登记历史授权日期 |
| 永久有效 | 不填到期日时列表不显示到期时间 |
| 删除 | 授权列表支持删除单条记录 |
| 授权状态 | 列表标注 `is_effective`（是否当前生效） |

**关键文件：**

| 文件 | 作用 |
|------|------|
| `app/services/authorization_service.py` | `_apply_supersession()`、生效授权判定 |
| `streamlit_app/05_authorization_management.py` | 授权管理 UI |
| `streamlit_app/authorization_utils.py` | 授权状态展示工具 |
| `tests/test_authorization_supersession.py` | 最新授权覆盖旧记录 |

### 4.4 仪器管理

| 功能 | 说明 |
|------|------|
| 去掉序列号/位置 | 迁移 `e7b2c4d8f1a3`；下拉显示「名称（型号）」 |
| 重复仪器合并 | `instrument_merge_service.py`：合并培训/考核/授权记录并重排仪器 ID |
| 合并后重排 | 仪器、培训、考核、授权 ID 一并重排 |

**关键文件：** `app/services/instrument_merge_service.py`、`streamlit_app/02_instrument_management.py`

### 4.5 日期筛选

培训、考核、授权列表由日期范围改为**单日筛选**（`on_date` 参数）。

| 文件 | 作用 |
|------|------|
| `app/services/date_range.py` | 后端单日过滤 |
| `streamlit_app/date_filter.py` | Streamlit 单日选择组件 |
| `tests/test_date_range_filter.py` | 筛选逻辑测试 |

### 4.6 数据库迁移（Alembic）

| 迁移文件 | 说明 |
|----------|------|
| `d4f8a1c2e6b0_allow_multiple_authorizations_per_pair.py` | 允许多次授权 |
| `e7b2c4d8f1a3_drop_instrument_serial_and_location.py` | 删除仪器序列号、位置字段 |

Docker 启动时 `docker-entrypoint.sh` 自动执行 `alembic upgrade head` + `python -m app.bootstrap`。

### 4.7 Streamlit 管理端（:8501）

| 页面 | 文件 |
|------|------|
| 仪器管理 | `streamlit_app/02_instrument_management.py` |
| 培训管理 | `streamlit_app/03_training_management.py` |
| 考核管理 | `streamlit_app/04_assessment_management.py` |
| 授权管理 | `streamlit_app/05_authorization_management.py` |
| 操作日志 | `streamlit_app/06_operation_logs.py` |
| 统计报表 | `streamlit_app/07_statistics_report.py` |
| **Excel 导入** | `streamlit_app/08_data_import.py` |

默认管理员：工号 **3267**，密码 **123456**（首次空库自动创建）。

### 4.8 Excel 批量导入

从 `科室：培训+考核+授权.xlsx` 导入历史培训、考核、授权记录（管理员专用）。

| 项目 | 说明 |
|------|------|
| 入口 | 培训管理端 → **Excel 导入** |
| API | `POST /api/v1/imports/lab-excel?dry_run=true/false` |
| 工作表 | 默认「仪器」，可选「仪器-备份」「岗位」 |
| 预览 | `dry_run=true` 仅统计，不写库 |
| 人员 | 按姓名匹配；不存在则创建（工号 `XL0001` 起，密码 `123456`） |
| 仪器 | 按名称匹配；不存在则自动创建 |
| 去重 | 同人 + 同仪器 + 同日期 跳过 |
| 导入后 | 自动重排培训/考核/授权 ID |

**关键文件：** `app/services/excel_import_service.py`、`tests/test_excel_import_service.py`

---

## 5. subi_knowledge_platform 后端

### 5.1 整合代理（BFF）

| 文件 | 作用 |
|------|------|
| `backend/config.py` | pydantic-settings 配置（读取 `.env`） |
| `backend/integrations/instrument_training.py` | httpx 异步客户端 + JWT 缓存 |
| `backend/integrations/routes.py` | 代理路由 |
| `backend/integrations/schemas.py` | 错误/批量响应模型 |

**代理接口：**

| 接口 | 说明 | 需登录 |
|------|------|--------|
| `GET /integrations/training/health` | 培训系统连通性 | 否 |
| `GET /integrations/training/instruments` | 仪器列表（下拉选择） | 是 |
| `GET /integrations/training/qualifications` | 按工号查资质 | 是 |
| `GET /integrations/training/qualifications/by-instrument` | 按仪器查人员 | 是 |
| `POST /integrations/training/qualifications/batch-check` | 批量核验（最多 50 条） | 是 |

### 5.2 资料版本管理

| 文件 | 作用 |
|------|------|
| `backend/document_versions.py` | `document_group_id` 迁移、版本组状态刷新 |
| `backend/document_compare.py` | 两版本元数据/正文对比，Word 页码定位 |
| `backend/models.py` | `Document.document_group_id` 字段 |

**资料相关 API：**

| 接口 | 说明 |
|------|------|
| `GET /documents/{id}/versions` | 按版本组返回全部版本 |
| `GET /documents/compare?doc_id_a=&doc_id_b=` | 两版本差异对比 |
| `POST /documents/groups/{id}/refresh-version-status` | 按生效日期刷新现行/废止 |
| `POST /documents` | 支持 `document_group_id`（上传新版本） |

**版本组规则：**

- 新建资料：自动 `document_group_id = 自身 id`
- 上传新版本：继承原资料 `document_group_id`
- 现行版本：同组内 **生效日期最晚** 者为「现行」，其余为「废止」
- 启动时自动迁移旧数据（按标题回填 group_id）

### 5.3 依赖说明

`backend/requirements.txt` 关键依赖：

- `httpx` — 培训系统代理
- `bcrypt>=4.0.0,<5.0.0` — 避免 Docker 登录 500
- `python-docx` — 版本对比时提取 Word 正文与页码

### 5.4 环境变量

```env
INSTRUMENT_TRAINING_API_BASE=http://localhost:8000/api/v1
INSTRUMENT_TRAINING_SERVICE_EMPLOYEE_ID=3267
INSTRUMENT_TRAINING_SERVICE_PASSWORD=123456
INSTRUMENT_TRAINING_TIMEOUT=10
```

---

## 6. Sub-I 前端（Streamlit）

### 6.1 关键文件

| 文件 | 作用 |
|------|------|
| `frontend/app.py` | 资料库主界面、版本管理、详情页 |
| `frontend/qualification_inquiry.py` | 资质查询（单人 / 批量核验） |

### 6.2 资质查询

| 功能 | 说明 |
|------|------|
| 按工号 / 按仪器查询 | 对接 BFF 代理 |
| 批量核验 | 选仪器 + 粘贴/上传工号，导出 Excel |
| 仪器下拉搜索 | 关键字筛选仪器列表 |
| 问题分项标注 | 缺培训 / 缺考核 / 考核未通过 / 未授权 / 授权过期；表格、卡片、批量核验与 Excel 导出均展示 |

**入口：** 侧边栏 → 「项目检测人员授权资质查询」

### 6.3 资料库与版本管理

| 功能 | 说明 |
|------|------|
| 独立资料详情页 | 列表/分类页「查看详情」跳转，支持返回来源页 |
| 版本时间线 | 查看历史版本详情、下载历史附件 |
| 版本对比 | 元数据差异表 + 正文左右两栏对照 |
| 页码定位 | Word 按分页符/渲染分页标记；TXT 按约 40 行/页估算 |
| 分类「全部版本」视图 | 展开显示所有历史版本记录 |
| 生效日期 | 编辑/上传新版本用 `date_input`；上传新资料可选填 |

### 6.4 版本对比使用说明

1. 进入资料详情页 → 滚至「版本对比」
2. 选择版本 A（基准）、版本 B（对照）→ 开始对比
3. 查看元数据差异表
4. 正文差异以 **左右两栏** 展示：
   - 左栏（橙色）：版本 A 旧内容 + 页码
   - 右栏（绿色）：版本 B 新内容 + 页码

> Word 页码为定位参考，取决于文档内分页符及 Word 保存时的渲染分页信息。

---

## 7. 部署与启动

### 7.1 一键启动

**Docker 全栈（家里 / 单位推荐）：**

```bat
C:\projects\start-docker.bat
```

等价命令：`cd C:\projects` → `docker compose up -d --build`

Docker Desktop 中显示为 **`projects`** 项目，内含 4 个容器（8000 / 8501 / 8001 / 8510）。

**本地 Python（仅开发调试）：**

```bat
C:\projects\subi_knowledge_platform\启动资料库系统.bat
```

或 `start-local.bat`（跳转到上述脚本）。若 Docker 已在运行，脚本会自动跳过 8000/8001/8510 的本地启动。

**特性：**

- 启动结束时健康检查 8000 / 8001 / 8510
- **Git 拉取代码后若用 Docker，务必重新运行 `start-docker.bat` 重建镜像**
- 默认登录工号 **3267**，密码 **123456**

### 7.2 访问地址

| 服务 | 地址 |
|------|------|
| Sub-I 前端 | http://localhost:8510 |
| Sub-I API 文档 | http://127.0.0.1:8001/docs |
| 培训系统 API 文档 | http://127.0.0.1:8000/docs |

### 7.3 Docker 统一部署（可选）

```bash
cd C:\projects
docker compose up -d --build
```

| 服务 | 端口 |
|------|------|
| instrument-training-home-backend | 8000 |
| instrument-training-home-frontend | 8501 |
| subi-backend | 8001 |
| subi-frontend | 8510 |

> 运行 `docker compose up -d --build` 或 `start-docker.bat` 即可启动全部服务，无需再单独启动本地 Streamlit。

**数据卷：**

| 路径 | 说明 |
|------|------|
| `instrument-training-home/data/` | 培训系统 SQLite（`instrument_training.db`） |
| `subi_knowledge_platform/data/` | Sub-I SQLite（`subi_knowledge.db`） |
| `subi_knowledge_platform/uploads/` | Sub-I 资料附件 |

**代码更新后重建培训后端：**

```powershell
cd C:\projects
docker compose up -d --build instrument-training-home-backend
```

---

## 7.4 数据库备份与恢复

一键备份脚本（复制数据库 + Sub-I 附件到带时间戳目录）：

```bat
C:\projects\备份数据.bat
```

或英文入口：`C:\projects\backup-data.bat`

| 备份内容 | 源路径 |
|----------|--------|
| 培训系统数据库 | `instrument-training-home/data/instrument_training.db` |
| Sub-I 数据库 | `subi_knowledge_platform/data/subi_knowledge.db` |
| Sub-I 附件 | `subi_knowledge_platform/uploads/` |

备份输出目录：`C:\projects\backups\yyyy-MM-dd_HHmmss\`

恢复：运行 `C:\projects\恢复数据.bat`（或 `restore-data.bat`），选择备份文件夹。

> 建议备份前停止 Docker / 本地服务（8000/8001/8510），避免 SQLite 写入冲突。若服务仍在运行，脚本会提示确认。

### 7.4.1 定时自动备份（推荐）

| 脚本 | 说明 |
|------|------|
| `install-scheduled-backup.bat` | 注册 Windows 计划任务，每天 02:00 自动备份（推荐） |
| `安装定时备份.bat` | 跳转到上述英文脚本（避免中文批处理编码问题） |
| `scheduled-backup.bat` | 静默备份入口（计划任务调用） |
| `uninstall-scheduled-backup.bat` | 移除计划任务 `ProjectsDataBackup` |
| `卸载定时备份.bat` | 跳转到上述卸载脚本 |
| `scripts/scheduled-backup.ps1` | 备份逻辑：Docker 在线备份 + 清理旧备份 |
| `scripts/sqlite_backup.py` | SQLite 在线备份辅助脚本 |

- 自动保留最近 **14** 份备份
- 日志：`backups/scheduled-backup.log`
- 首次安装：双击 `安装定时备份.bat`（建议管理员运行）

---

## 7.5 家里 / 单位换机部署指南

### 首次在新电脑搭建

1. 安装 **Docker Desktop** 并确保已启动
2. 克隆三个仓库到 `C:\projects`（见 §3 克隆示例）
3. 复制环境变量：
   ```bat
   copy C:\projects\.env.example C:\projects\subi_knowledge_platform\.env
   ```
   编辑 `.env`，填写培训系统服务账号（默认工号 3267 / 密码 123456）
4. **恢复业务数据**（从 U 盘 / 网盘 / 另一台电脑的备份）：
   ```bat
   C:\projects\恢复数据.bat
   ```
   选择 `backups\yyyy-MM-dd_HHmmss\` 备份文件夹
5. 启动全栈：
   ```bat
   C:\projects\start-docker.bat
   ```

### 日常启动（家里和单位相同）

**只需记住一个：**

```bat
C:\projects\启动系统.bat
```

（等同 `start-docker.bat`）访问 http://127.0.0.1:8510（Sub-I 资料库）、http://127.0.0.1:8501（培训管理）。

**关机停止容器（可选）：** `停止系统.bat`

### 离开当前电脑前（同步数据）

```bat
C:\projects\备份数据.bat
```

将 `C:\projects\backups\` 下最新时间戳文件夹复制到 U 盘或网盘。

### 7.6 单位离线电脑一键部署（不联网）

适用于单位电脑**无法访问 GitHub / 无法 docker pull** 的场景。在家里联网电脑打好「离线部署包」，U 盘带走后在单位双击部署。

| 脚本 | 在哪执行 | 作用 |
|------|----------|------|
| `导出离线部署包.bat` | 家里（Docker 已启动、可联网） | 打包代码 + 镜像 + 数据 |
| `deploy-offline.bat` / `离线部署.bat` | 单位（部署包目录内） | 加载镜像、恢复数据、启动全栈 |

**家里（导出）：**

```bat
C:\projects\导出离线部署包.bat
```

生成目录：`backups\offline-deploy_yyyy-MM-dd_HHmmss\`，包含：

| 内容 | 说明 |
|------|------|
| `projects\` | 三仓库 + compose 脚本快照 |
| `docker-images.tar` | 4 个已构建容器镜像（单位无需再 build） |
| `data-backup\` | 业务库与附件（可选跳过，仅更新代码时选 Y） |
| `离线部署.bat` | 单位一键入口 |

将整个 `offline-deploy_*` 文件夹复制到 U 盘。

**单位（部署，首次需安装 Docker Desktop）：**

1. U 盘拷贝 `offline-deploy_*` 到本机（任意路径均可）
2. 双击包内 **`离线部署.bat`**
3. 按提示选择目标目录（默认 `C:\projects` 或 `D:\projects`）
4. 访问 http://127.0.0.1:8510 / http://127.0.0.1:8501

> 单位电脑全程不需要 `git clone`、`git pull` 或访问外网。Docker Desktop 安装包可提前在有网电脑下载后拷过去安装。

**仅同步数据、不更新代码时：** 仍可用 `备份数据.bat` → `恢复数据.bat`，比打完整离线包更快。

### 代码更新（Git 拉取后）

```powershell
cd C:\projects
git pull

cd C:\projects\instrument-training-home
git pull

cd C:\projects\subi_knowledge_platform
git pull

cd C:\projects
docker compose up -d --build
```

### 关键原则

| 内容 | 同步方式 |
|------|----------|
| 程序代码 | Git（三个仓库分别 `git pull`） |
| 培训/授权/资料数据 | **备份脚本**（不会随 Git 自动同步） |
| Docker 镜像 | 每台电脑本地构建（`--build`） |

### 路径自动识别（C:\projects / D:\projects）

以下脚本通过 `scripts\resolve-projects-dir.bat` 自动定位工作区，**无需改盘符**：

| 脚本 | 用途 |
|------|------|
| `start-docker.bat` | Docker 全栈启动 |
| `backup-data.bat` / `备份数据.bat` | 数据备份 |
| `restore-data.bat` / `恢复数据.bat` | 数据恢复 |
| `subi_knowledge_platform\启动资料库系统.bat` | 本地 Python 启动（自动识别上级 projects 目录） |

---

## 8. 日常使用

### 资质查询

1. 登录 → 侧边栏「项目检测人员授权资质查询」
2. 单人查询或「批量核验」标签页
3. 仪器优先用下拉搜索选择

### 资料版本

1. 资料列表 / 分类详情 → 「查看详情」
2. 版本时间线查看历史版本
3. 「版本对比」左右对照核查差异
4. 上传新版本时选择生效日期（日历控件）

---

## 9. 问题排查记录

| 问题 | 原因 | 处理 |
|------|------|------|
| 登录 500（Docker） | `bcrypt 5.x` 与 `passlib` 不兼容 | 锁定 `bcrypt<5`，重建 `subi_backend` |
| 批量核验「请求参数有误」 | 同时传 `instrument_id` + `instrument_name` | 前端优先只传 `instrument_id` |
| 培训系统连接失败 | 8000 未启动或 Docker 未起 | 运行一键启动 / `start-docker.bat` |
| 版本对比无页码 | 附件非 docx/txt | 仅 docx/txt 支持正文对比；页码对 txt 为估算值 |
| 按仪器查询失败 | 8000 运行旧代码 | 重启培训后端 |
| 培训/授权 ID 与日期不一致 | 历史数据未重排或后端未重建 | `docker compose up -d --build instrument-training-home-backend` |
| 授权多条记录不知哪条生效 | — | 看列表「授权状态」列，`is_effective=true` 为当前生效 |
| 培训记录编辑报错 | `instrument_id` 为空 | 已修复；编辑时仪器字段必填 |

---

## 10. 交付清单

| 类别 | 状态 |
|------|------|
| 资质查询（单人 + 批量 + 仪器下拉） | 已完成 |
| BFF 代理端点 | 6 个 |
| 资料版本组 `document_group_id` | 已完成 |
| 版本对比 API + 左右对照 UI | 已完成 |
| 独立详情页 + 历史版本下载 | 已完成 |
| 生效日期 `date_input` | 已完成 |
| 一键启动 + Docker 脚本 | 已完成 |
| 培训/考核/授权 ID 按日期重排 | 已完成 |
| 授权多次登记 + 最新日期生效 | 已完成 |
| 仪器去序列号/位置 + 重复合并 | 已完成 |
| 单日日期筛选 | 已完成 |
| 数据库备份/恢复脚本 | 已完成 |
| 路径自动识别脚本 | 已完成 |
| Excel 批量导入 | 已完成 |
| 资质查询问题分项标注 | 已完成 |
| 离线部署包导出 / 一键部署 | 已完成 |
| 定时自动备份（计划任务） | 已完成 |
| 首次登录强制改密（两系统） | 已完成 |
| 授权到期提醒独立页 + 导出 | 已完成 |
| 操作日志日期筛选 | 已完成 |
| 统计报表 limit 提升 + 首页培训概览 | 已完成 |
| Excel 导入失败行导出 | 已完成 |
| Sub-I 全文检索（标题/标签/正文） | 已完成 |
| PDF 版本对比 | 已完成 |
| Streamlit width 参数迁移 | 已完成 |
| 仪器下拉缓存自动刷新 | 已完成 |
| 批量核验 Excel 增强（元数据 sheet） | 已完成 |

---

## 11. 后续待办（可选）

- [x] 批量核验前端
- [x] 仪器下拉搜索
- [x] `document_group_id` 版本管理
- [x] 独立资料详情页
- [x] 历史版本查看/下载
- [x] 版本对比（左右对照 + 页码定位）
- [x] 生效日期 `date_input`
- [x] 启动脚本健康检查
- [x] Sub-I Streamlit 前端容器化（:8510）
- [x] `app.py` 中其余 `use_container_width` 替换为 `width` 参数
- [x] PDF 正文对比支持
- [x] 定时自动备份
- [x] 首次登录强制改密
- [x] 授权到期提醒页
- [x] 操作日志日期筛选
- [x] 全文检索
- [x] Excel 导入失败行导出
- [x] 培训/考核/授权 ID 按日期重排
- [x] 授权多次登记与最新授权生效
- [x] 仪器管理简化（去序列号/位置）与重复合并
- [x] 培训/考核/授权单日筛选
- [x] 启动脚本 C/D 盘路径自动识别
- [x] Excel 批量导入（培训/考核/授权）
- [x] 资质查询结果分项标注（缺培训/缺考核/授权过期等）

---

## 12. 相关文档

| 文档 | 位置 |
|------|------|
| **整合交接（本文档）** | `C:\projects\项目整合交接文档.md` |
| Sub-I 资料库功能交接 | `subi_knowledge_platform/项目交接总结.md` |
| Sub-I README | `subi_knowledge_platform/README.md` |
| 培训系统 README | `instrument-training-home/README.md` |
| 环境变量模板 | `.env.example` |

> 已删除 `subi_knowledge_platform/项目整合交接文档.md` 旧副本，避免与根目录文档重复。

---

## 13. 新对话快速上手

```text
请阅读 C:\projects\项目整合交接文档.md 和 subi_knowledge_platform/项目交接总结.md，
在此基础上继续维护 Sub-I 与仪器培训系统的整合功能。
Docker 一键启动：C:\projects\start-docker.bat（家里/单位相同）
访问：Sub-I http://127.0.0.1:8510 | 培训管理 http://127.0.0.1:8501
换机数据同步：备份数据.bat → 恢复数据.bat
默认账号：工号 3267，密码 123456
```