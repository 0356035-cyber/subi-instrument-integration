# Sub-I 与仪器培训系统整合 — 项目交接文档

> 更新日期：2026-06-22  
> 交接范围：`subi_knowledge_platform` + `instrument-training-home` API 整合

---

## 1. 整合目标与成果

将 **Sub-I 临床研究资料库** 与 **仪器培训授权系统（instrument-training-home）** 打通，形成可用的「**项目检测人员授权资质查询**」功能。

| 阶段 | 状态 |
|------|------|
| 预留入口、未接入 | 已完成升级 |
| 双模式查询 + BFF 代理 + 一键启动 | **已上线可用** |

---

## 2. 整体架构

```
用户浏览器 (Streamlit :8510)
        ↓ Bearer Token（Sub-I 用户登录态）
Sub-I 后端 (FastAPI :8001)          ← BFF 代理层，隐藏培训系统服务账号
        ↓ 服务账号 JWT
instrument-training-home (FastAPI :8000)
        ↓
SQLite 数据库
```

**设计要点：**

- Sub-I 前端**只访问自己的 8001 后端**，不直连培训系统
- 培训系统服务账号凭证保存在 `subi_knowledge_platform/.env`，**不暴露给前端**
- 两系统以 **工号（`employee_id`）** 作为人员关联主键

---

## 3. GitHub 仓库

| 仓库 | 地址 | 内容 |
|------|------|------|
| subi_knowledge_platform | https://github.com/0356035-cyber/subi_knowledge_platform | Sub-I 主体（BFF + Streamlit） |
| instrument-training-home | https://github.com/0356035-cyber/instrument-training-home | 仪器培训与授权系统 |
| subi-instrument-integration | https://github.com/0356035-cyber/subi-instrument-integration | 根目录统一部署配置 |

**克隆整合工作区示例：**

```bash
git clone https://github.com/0356035-cyber/subi-instrument-integration.git projects
cd projects
git clone https://github.com/0356035-cyber/subi_knowledge_platform.git
git clone https://github.com/0356035-cyber/instrument-training-home.git
```

---

## 4. instrument-training-home 侧改动

### 4.1 新增文件

| 文件 | 作用 |
|------|------|
| `app/schemas/integration.py` | 聚合响应 Pydantic 模型 |
| `app/services/integration_service.py` | 资质聚合业务逻辑 |
| `app/api/v1/integrations.py` | 对外整合路由 |

### 4.2 修改文件

| 文件 | 改动 |
|------|------|
| `app/core/permissions.py` | `can_query_user_by_employee_id`、`can_query_qualifications_by_instrument` |
| `app/services/user_service.py` | `get_user_by_employee_id` |
| `app/main.py` | 注册 integrations 路由、`GET /health` |

### 4.3 新增 API

| 接口 | 说明 |
|------|------|
| `GET /api/v1/integrations/qualifications` | 按工号查单人资质（授权 + 培训 + 考核 + 汇总） |
| `GET /api/v1/integrations/qualifications/by-instrument` | 按仪器查具备资质的人员列表 |
| `GET /api/v1/users/by-employee-id/{employee_id}` | 按工号查用户（P1） |

API 文档：http://127.0.0.1:8000/docs

---

## 5. subi_knowledge_platform 后端（BFF 代理）

### 5.1 新增文件

| 文件 | 作用 |
|------|------|
| `backend/config.py` | pydantic-settings 配置（读取 `.env`） |
| `backend/integrations/instrument_training.py` | httpx 异步客户端 + JWT 缓存 |
| `backend/integrations/routes.py` | 代理路由 |
| `backend/integrations/schemas.py` | 错误/批量响应模型 |
| `Dockerfile.backend` | Sub-I 后端容器化 |
| `.gitignore` | 排除 `.env`、`.venv`、`data/` 等 |

### 5.2 修改文件

| 文件 | 改动 |
|------|------|
| `backend/main.py` | 注册代理路由、新增 `GET /health` |
| `backend/requirements.txt` | 新增 `httpx` |

### 5.3 代理接口

| 接口 | 说明 | 需登录 |
|------|------|--------|
| `GET /health` | Sub-I 自身健康检查 | 否 |
| `GET /integrations/training/health` | 培训系统连通性 | 否 |
| `GET /integrations/training/qualifications` | 按工号查资质 | 是 |
| `GET /integrations/training/qualifications/by-instrument` | 按仪器查人员 | 是 |
| `POST /integrations/training/qualifications/batch-check` | 批量核验 | 是 |

API 文档：http://127.0.0.1:8001/docs

### 5.4 环境变量

复制根目录 `.env.example` 为 `subi_knowledge_platform/.env`：

```env
INSTRUMENT_TRAINING_API_BASE=http://localhost:8000/api/v1
INSTRUMENT_TRAINING_SERVICE_EMPLOYEE_ID=3267
INSTRUMENT_TRAINING_SERVICE_PASSWORD=123456
INSTRUMENT_TRAINING_TIMEOUT=10
```

> Docker 部署时，`docker-compose.yml` 会将 `INSTRUMENT_TRAINING_API_BASE` 覆盖为内网地址 `http://instrument-training-home-backend:8000/api/v1`。

---

## 6. Sub-I 前端（Streamlit）

### 6.1 新增/修改

| 文件 | 作用 |
|------|------|
| `frontend/qualification_inquiry.py` | 资质查询页面（新建） |
| `frontend/app.py` | 侧边栏入口、路由 |
| `frontend/requirements.txt` | 新增 `openpyxl`（Excel 导出） |

### 6.2 页面功能

| 功能 | 说明 |
|------|------|
| 按工号查询 | 查某人在各仪器的授权/培训/考核资质 |
| 按仪器查询 | 查某仪器下有哪些人具备资质 |
| 状态标签 | 有效(绿)、已过期(红)、未授权(灰)、考核未通过(橙) |
| 三 Tab 展示 | 概览 / 表格视图 / 卡片视图 |
| 快捷筛选 | 按资质状态过滤 |
| 导出 Excel | 表格视图顶部「导出为 Excel」 |
| 错误/空态 | 友好提示与排查建议 |

**入口：** 侧边栏 → 「项目检测人员授权资质查询」

---

## 7. 部署与启动

### 7.1 一键启动（推荐，本地 Python）

```bat
C:\projects\subi_knowledge_platform\启动资料库系统.bat
```

或双击 `C:\projects\start-local.bat`（跳转至上述脚本）。

脚本会依次启动：

1. instrument-training-home 后端（8000）
2. Sub-I 后端（8001）
3. Sub-I 前端 Streamlit（8510）

**特性：**

- 不依赖 Docker，本地 Python 启动培训系统
- 自动检测 8000 是否含最新「按仪器查询」接口
- 端口占用时跳过重复启动

### 7.2 访问地址

| 服务 | 地址 |
|------|------|
| Sub-I 前端 | http://localhost:8510 |
| Sub-I API 文档 | http://127.0.0.1:8001/docs |
| 培训系统 API 文档 | http://127.0.0.1:8000/docs |

### 7.3 Docker 统一部署（可选）

在 `C:\projects` 目录：

```bash
docker compose up -d --build
```

| 服务 | 端口 |
|------|------|
| instrument-training-home-backend | 8000 |
| instrument-training-home-frontend | 8501 |
| subi-backend | 8001 |

> Sub-I Streamlit 前端（8510）**未容器化**，仍需本地运行。

---

## 8. 日常使用

1. 运行一键启动脚本
2. 浏览器打开 http://localhost:8510
3. 使用 Sub-I 账号登录
4. 侧边栏 → 「项目检测人员授权资质查询」
5. 选择「按工号查询」或「按仪器查询」，填写条件后点击「查询资质」

---

## 9. 问题排查记录

| 问题 | 原因 | 处理 |
|------|------|------|
| 健康检查 `Could not validate credentials` | 培训系统无公开 `/health`，健康检查需登录 | 新增 `/health`，培训健康检查免登录 |
| subi-backend 连接被拒绝 | `routes.py` 误删 models 导入导致崩溃 | 恢复导入并重建容器 |
| 502 / localhost 拒绝连接 | 培训系统 8000 未启动 | 合并启动脚本 |
| PowerShell `curl` 异常 | 别名非真 curl | 改用 `curl.exe` |
| `streamlit_extras` 缺失 | 无用导入 + 未安装依赖 | 删除无用导入 |
| Excel 导出按钮不可见 | 按钮在窄列布局中 | 导出区置顶 + 独立按钮 |
| 按仪器查询失败 | 8000 运行旧代码，无 `by-instrument` 接口 | 重启培训后端 + 启动脚本版本检测 |
| Streamlit Arrow 序列化报错 | `理论成绩` 列混用数字与 `"—"` | `_display_optional_number()` 统一为字符串 |
| `use_container_width` 弃用警告 | Streamlit 新版 API 变更 | 改用 `width="stretch"` / `width="content"` |

---

## 10. 交付清单

| 类别 | 数量/状态 |
|------|-----------|
| 新增 API 端点（培训系统） | 2 个核心聚合接口 |
| 新增代理端点（Sub-I） | 5 个 |
| 新增/修改代码文件 | 约 20+ 个 |
| 前端查询页 | 完整可用（双模式 + 导出） |
| 一键启动 | 培训系统 + Sub-I 全自动 |
| Docker 编排 | 可选，根目录 `docker-compose.yml` |

---

## 11. 后续待办（可选）

- [ ] 批量核验 API 的前端入口（后端已提供 `batch-check`）
- [ ] Sub-I Streamlit 前端容器化
- [ ] `app.py` 中其余 `use_container_width` 批量替换为新 `width` 参数
- [ ] 将 `document_group_id` 引入资料版本管理（见 `项目交接总结.md`）

---

## 12. 相关文档

| 文档 | 位置 |
|------|------|
| Sub-I 资料库功能交接 | `subi_knowledge_platform/项目交接总结.md` |
| Sub-I README | `subi_knowledge_platform/README.md` |
| 培训系统 README | `instrument-training-home/README.md` |
| 环境变量模板 | `.env.example` |

---

## 13. 新对话快速上手

新开 AI 对话时，可直接说明：

```text
请阅读 C:\projects\项目整合交接文档.md 和 subi_knowledge_platform/项目交接总结.md，
在此基础上继续维护 Sub-I 与仪器培训系统的整合功能。
当前一键启动脚本：subi_knowledge_platform\启动资料库系统.bat
```