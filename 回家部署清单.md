# 回家部署清单（极空间 Z4 Pro + Cloudflare Tunnel）

> 适用场景：离线包已拷到极空间，人回到家后与 NAS **同一局域网** 内完成部署与外网验收。  
> 离线包路径（Windows 挂载）：`Z:\sata11-13816937329\docker\offline-deploy_2026-06-26_122518`  
> 默认登录：工号 **3267**，密码 **123456**（部署成功后请立即修改）

---

## 0. 出发前检查（可选）

- [ ] 离线包文件夹已在极空间：`docker/offline-deploy_2026-06-26_122518`
- [ ] 包内包含：`projects\`、`docker-images.tar`（约 512 MB）、`data-backup\`、`deploy-offline.bat`
- [ ] Cloudflare 域名状态为 **Active**
- [ ] 记下极空间 **局域网 IP**（客户端或路由器里查看，下文示例用 `192.168.1.50`，请替换）

---

## 1. 网络与环境（回家第一件事）

### 1.1 必须在同一局域网

以下操作都需要电脑和极空间连 **同一个 WiFi/路由器**：

- 挂载 Z 盘
- SSH 登录极空间
- 内网浏览器访问 `:8501`、`:8510`

### 1.2 关闭 Windows 上的 Docker Desktop

**不要**在 Windows 上双击 Z 盘里的 `deploy-offline.bat` 来部署——那样容器会起在电脑上，而不是极空间。

回家后可关闭本机 Docker Desktop，避免误操作。

### 1.3 开启极空间 SSH

1. 极空间 **设置 → SSH**
2. 开启 SSH，记下：
   - **局域网 IP**（例：`192.168.1.50`）
   - **端口**（极空间多为 `10000`，不是 22）
3. 建议勾选 **「仅限局域网访问」**

> SSH 设置页**没有**命令行窗口；命令在 **Windows 电脑** 的终端里输入，远程连到极空间执行。

---

## 2. 阶段 B：极空间部署 Docker（SSH）

### 2.1 从 Windows 连接极空间

`Win + X` → **终端** 或 **PowerShell**，执行（替换 IP 和手机号）：

```powershell
ssh -p 10000 13816937329@192.168.1.50
```

- 用户名：极空间登录 **手机号**（`13816937329`）
- 密码：极空间登录密码（输入时不显示，正常）
- 首次连接输入 `yes` 信任主机

若提示找不到 `ssh`：Windows **设置 → 应用 → 可选功能** → 安装 **OpenSSH 客户端**。

### 2.2 提权到 root

SSH 连上后执行：

```bash
sudo -i
```

再输入一次密码，提示符变为 `root@` 即可操作 Docker。

### 2.3 进入离线包目录

```bash
cd /tmp/zfsv3/sata11/13816937329/data/docker/offline-deploy_2026-06-26_122518
ls
```

应看到：`projects`  `docker-images.tar`  `data-backup`  `deploy-offline.bat` 等。

**若 `cd` 失败**，查找实际路径：

```bash
find /tmp/zfsv3 -name "offline-deploy_2026-06-26_122518" 2>/dev/null
```

记下输出路径，替换下面脚本中的 `PKG` 变量。

### 2.4 一键部署（推荐，复制整段执行）

```bash
PKG="/tmp/zfsv3/sata11/13816937329/data/docker/offline-deploy_2026-06-26_122518"
TARGET="/tmp/zfsv3/sata11/13816937329/data/docker/projects"

echo "=== [1/5] 复制代码到运行目录 ==="
mkdir -p "$TARGET"
cp -r "$PKG/projects/"* "$TARGET/"

echo "=== [2/5] 导入 Docker 镜像（约 1～3 分钟）==="
docker load -i "$PKG/docker-images.tar"

echo "=== [3/5] 恢复业务数据 ==="
mkdir -p "$TARGET/instrument-training-home/data"
mkdir -p "$TARGET/subi_knowledge_platform/data"
mkdir -p "$TARGET/subi_knowledge_platform/uploads"

if [ -f "$PKG/data-backup/instrument_training.db" ]; then
  cp "$PKG/data-backup/instrument_training.db" "$TARGET/instrument-training-home/data/"
  echo "  [OK] instrument_training.db"
fi
if [ -f "$PKG/data-backup/subi_knowledge.db" ]; then
  cp "$PKG/data-backup/subi_knowledge.db" "$TARGET/subi_knowledge_platform/data/"
  echo "  [OK] subi_knowledge.db"
fi
if [ -d "$PKG/data-backup/uploads" ]; then
  cp -r "$PKG/data-backup/uploads/"* "$TARGET/subi_knowledge_platform/uploads/" 2>/dev/null || true
  echo "  [OK] uploads"
fi

echo "=== [4/5] 启动四容器 ==="
cd "$TARGET"
docker compose up -d --no-build

echo "=== [5/5] 查看状态 ==="
sleep 8
docker compose ps
```

### 2.5 内网验收（阶段 B 完成标志）

在同一局域网的浏览器访问（**IP 换成你的极空间局域网 IP**）：

| 服务 | 地址 |
|------|------|
| 培训系统 | http://192.168.1.50:8501 |
| Sub-I 资料库 | http://192.168.1.50:8510 |

检查项：

- [ ] 四个容器均为 `running`（`subi_backend` 为 `healthy`）
- [ ] 两个页面能打开并登录（3267 / 123456）
- [ ] 培训列表、授权、Sub-I 资料库各试一次
- [ ] **不要**在路由器上映射 8501/8510 到公网

排查命令（SSH 里执行）：

```bash
cd /tmp/zfsv3/sata11/13816937329/data/docker/projects
docker compose logs --tail 50
docker compose ps
```

---

## 3. 阶段 C：Cloudflare Tunnel（内网通过后再做）

### 3.1 创建 Tunnel

1. 浏览器打开 [Cloudflare Zero Trust](https://one.dash.cloudflare.com/)（与添加域名同一账号）
2. **Networks** → **Tunnels** → **Create a tunnel**
3. 名称：`zspace-nas`（可自定）
4. 安装方式选 **Docker**，复制 **Tunnel Token**（只显示一次，存到记事本）

### 3.2 配置 Public Hostname（两条）

在同一 Tunnel 页面 → **Public Hostname** → **Add a public hostname**：

| Subdomain | Domain | Type | URL |
|-----------|--------|------|-----|
| `train` | 你的域名 | HTTP | `http://127.0.0.1:8501` |
| `subi` | 你的域名 | HTTP | `http://127.0.0.1:8510` |

外网地址示例：

- `https://train.你的域名`
- `https://subi.你的域名`

### 3.3 Cloudflare 控制台建议设置

在域名总览：

- **SSL/TLS** → 加密模式：**Full**
- **Speed** → 如有 **Rocket Loader**，建议关闭

### 3.4 在极空间配置 Token

SSH 连上极空间（`sudo -i` 后）：

```bash
cd /tmp/zfsv3/sata11/13816937329/data/docker/projects

cp .env.tunnel.example .env.tunnel
```

编辑 `.env.tunnel`（可用 `vi` 或极空间文件管理器改）：

```env
TUNNEL_TOKEN=你从Cloudflare复制的完整Token
```

> `.env.tunnel` 含密钥，勿泄露、勿提交 Git。

### 3.5 启动 Tunnel

```bash
cd /tmp/zfsv3/sata11/13816937329/data/docker/projects

docker compose --env-file .env.tunnel -f docker-compose.yml -f docker-compose.tunnel.yml up -d
docker logs -f cloudflared
```

成功标志：日志出现 **`Registered tunnel connection`**；Zero Trust 仪表盘 Connector 为 **Healthy**。

按 `Ctrl+C` 退出日志查看（容器继续运行）。

### 3.6 外网验收（最终看效果）

手机 **关闭 WiFi，用流量** 访问：

- [ ] `https://train.你的域名` 能打开并登录
- [ ] `https://subi.你的域名` 能打开并登录
- [ ] 列表、导出、考核→授权跳转正常
- [ ] 外网无法访问 `:8000`、`:8001`（API 不应对外）

---

## 4. 常见问题

### 4.1 Tunnel 连不上 / 502

极空间可能不支持 `network_mode: host`。编辑 `docker-compose.tunnel.yml` 中 `cloudflared` 段，并改 Cloudflare Public Hostname 的 URL：

- `http://host.docker.internal:8501`
- `http://host.docker.internal:8510`

仍不通则改用极空间局域网 IP，例如 `http://192.168.1.50:8501`。

详见项目内 `极空间部署说明.md` §6.1。

### 4.2 页面能开但卡顿 / WebSocket 断开

- Cloudflare SSL 设为 **Full**
- 关闭 Rocket Loader

### 4.3 外网慢

主要受家庭宽带上行限制；日常使用通常可接受。

### 4.4 以后更新版本

在 Windows 开发机重新 `导出离线部署包.bat`，拷到极空间后 SSH 执行：

```bash
docker load -i /path/to/docker-images.tar
cd /tmp/zfsv3/sata11/13816937329/data/docker/projects
docker compose up -d --no-build
```

---

## 5. 安全上线（部署成功后必做）

- [ ] 修改默认密码（3267 管理员）
- [ ] `subi_knowledge_platform/.env` 服务账号使用强密码
- [ ] 确认 API 端口 8000/8001 未暴露到 Tunnel 或路由器
- [ ] `.env.tunnel` 妥善保管
- [ ] 极空间对 `data/`、`uploads/` 开启快照或定期备份
- [ ] （可选）Zero Trust → **Access** 限制邮箱白名单访问
- [ ] 与医院信息科确认业务数据允许外网访问

---

## 6. 快速命令索引

```bash
# SSH 连接（Windows 终端）
ssh -p 10000 13816937329@192.168.1.50

# 提权
sudo -i

# 项目目录
cd /tmp/zfsv3/sata11/13816937329/data/docker/projects

# 仅业务容器
docker compose up -d

# 业务 + Tunnel
docker compose --env-file .env.tunnel -f docker-compose.yml -f docker-compose.tunnel.yml up -d

# 查看状态 / 日志
docker compose ps
docker logs cloudflared --tail 50
docker compose logs --tail 50
```

---

## 7. 部署流程总览

```
回家 → 同一 WiFi
  → 关 Windows Docker Desktop
  → 开极空间 SSH
  → Windows 终端 ssh 连 NAS
  → sudo -i → 执行 §2.4 部署脚本
  → 内网测 :8501 / :8510
  → Cloudflare 建 Tunnel + 两条 Hostname
  → 写 .env.tunnel → 启动 cloudflared
  → 手机流量测 HTTPS
  → 改密 + 备份
```

---

## 8. 相关文件（项目内）

| 文件 | 作用 |
|------|------|
| `极空间部署说明.md` | 完整架构与说明 |
| `docker-compose.yml` | 四容器主配置 |
| `docker-compose.tunnel.yml` | Cloudflare Tunnel 叠加 |
| `.env.tunnel.example` | Tunnel Token 模板 |
| `backups/offline-deploy_2026-06-26_122518/` | 本次离线包（Windows 侧副本） |

---

*文档生成日期：2026-06-26。回家部署时请将文中 `192.168.1.50` 替换为实际极空间局域网 IP。*