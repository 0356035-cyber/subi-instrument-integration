# 极空间 Z4 Pro + Cloudflare Tunnel 部署说明

> 适用：极空间 NAS Docker 部署本系统，**无公网 IP**，通过 Cloudflare Tunnel 外网 HTTPS 访问。  
> 硬件参考：Z4 Pro、16GB 内存（四容器绰绰有余）。

---

## 1. 架构一览

```
外网用户 (HTTPS)
    ↓
Cloudflare（DNS + 证书）
    ↓
Cloudflare Tunnel（cloudflared 容器，无需公网 IP）
    ↓
极空间宿主机 127.0.0.1:8501 / :8510
    ↓
docker compose 四容器 + data/uploads 数据卷
```

| 服务 | 容器端口 | 是否对外 |
|------|----------|----------|
| 培训管理页面 | 8501 | ✅ 经 Tunnel |
| Sub-I 资料库 | 8510 | ✅ 经 Tunnel |
| 培训 API | 8000 | ❌ 仅 Docker 内网 |
| Sub-I API | 8001 | ❌ 仅 Docker 内网 |

推荐子域名：

- `https://train.你的域名.com` → 培训系统
- `https://subi.你的域名.com` → Sub-I 资料库

---

## 2. 准备清单

### 2.1 账号与域名（首次约 1～2 天）

- [ ] 购买域名（建议 `.cn` 或 `.com`，阿里云/腾讯云等，完成实名）
- [ ] 注册 [Cloudflare](https://dash.cloudflare.com/sign-up)（选 **Free** 计划）
- [ ] Cloudflare 添加站点 → 按提示把域名 **NS** 改到 Cloudflare 提供的两个名称服务器
- [ ] 等待状态变为 **Active**

### 2.2 极空间侧

- [ ] 已安装并启用 **Docker**
- [ ] 确认 NAS 能访问互联网（拉镜像、连接 Cloudflare）
- [ ] 规划目录，例如：`/volume1/docker/projects/`（以极空间实际路径为准）
- [ ] 建议为 `data/`、`uploads/` 所在共享目录开启 **快照/定期备份**

### 2.3 开发机（Windows，用于打包）

- [ ] 已安装 Docker Desktop
- [ ] 已有完整 `projects` 工作区（三个 Git 仓库）
- [ ] 已配置 `subi_knowledge_platform\.env`（服务账号，见 `.env.example`）

---

## 3. 阶段 A：在 Windows 上制作离线部署包

### A1 更新代码

```powershell
cd C:\projects
git pull
cd instrument-training-home
git pull
cd ..\subi_knowledge_platform
git pull
```

### A2 配置 Sub-I 环境变量

```powershell
copy subi_knowledge_platform\.env.example subi_knowledge_platform\.env
# 编辑 .env，填写 INSTRUMENT_TRAINING_SERVICE_EMPLOYEE_ID / PASSWORD
```

### A3 本地验证（建议）

双击 `启动系统.bat`，确认：

- http://127.0.0.1:8501 培训系统可登录
- http://127.0.0.1:8510 Sub-I 可登录
- **修改默认密码**（工号 3267 / 123456）

### A4 导出离线包

双击 **`导出离线部署包.bat`**（或 `export-offline-package.bat`）。

输出目录示例：

```
backups\offline-deploy_2026-06-25_120000\
├── projects\              # 代码快照（含 docker-compose*.yml）
├── docker-images.tar      # 四个业务镜像
├── data-backup\           # 业务数据（若选择包含）
├── deploy-offline.bat
└── deploy-offline.ps1
```

### A5 拷贝到极空间

将整个 `offline-deploy_*` 文件夹复制到极空间共享盘（或 U 盘中转）。

---

## 4. 阶段 B：极空间部署 Docker

### B1 一键离线部署

在极空间（若支持 SSH 或挂载共享盘后从 PC 操作）：

1. 进入离线包目录
2. 运行 **`离线部署.bat`** / `deploy-offline.bat`
3. 按提示选择目标目录（如 `D:\projects` 或 `/volume1/docker/projects`）

脚本会：`docker load` 导入镜像 → 复制 `projects` 快照 → 恢复数据。

### B2 手动部署（无 bat 时）

```bash
cd /volume1/docker/projects   # 你的实际路径

docker load -i /path/to/docker-images.tar
docker compose up -d
docker compose ps
```

### B3 内网验收

在同一局域网电脑浏览器访问：

```
http://<极空间内网IP>:8501
http://<极空间内网IP>:8510
```

四个容器均应为 `running`（`subi_backend` 需 healthy 后 `subi_frontend` 才就绪）。

**不要**在路由器上映射 8501/8510 到公网（你无公网 IP，且不安全）。

---

## 5. 阶段 C：配置 Cloudflare Tunnel

### C1 创建 Tunnel

1. 登录 [Cloudflare Zero Trust](https://one.dash.cloudflare.com/)
2. **Networks** → **Tunnels** → **Create a tunnel**
3. 名称：`zspace-nas`
4. 安装方式选 **Docker**，复制 **Tunnel Token**（只显示一次，妥善保存）

### C2 配置 Public Hostname

在同一 Tunnel 页面 → **Public Hostname** → **Add a public hostname**：

| Subdomain | Domain | Type | URL |
|-----------|--------|------|-----|
| `train` | 你的域名 | HTTP | `http://127.0.0.1:8501` |
| `subi` | 你的域名 | HTTP | `http://127.0.0.1:8510` |

保存后外网地址为：

- `https://train.你的域名.com`
- `https://subi.你的域名.com`

### C3 在极空间配置 Token

在 `projects` 根目录：

```bash
cp .env.tunnel.example .env.tunnel
# 编辑 .env.tunnel，填入：
# TUNNEL_TOKEN=你从Cloudflare复制的Token
```

`.env.tunnel` 已加入 `.gitignore`，**勿提交 Git**。

### C4 启动 Tunnel

**Windows / 支持 bat 的环境：**

```bat
启动Tunnel.bat
```

或手动：

```bash
docker compose --env-file .env.tunnel -f docker-compose.yml -f docker-compose.tunnel.yml up -d
docker logs -f cloudflared
```

日志出现 `Registered tunnel connection` 表示成功。

### C5 外网验收

- [ ] 手机关闭 WiFi，用流量打开 `https://train.你的域名.com`
- [ ] 打开 `https://subi.你的域名.com`
- [ ] 登录、列表、导出各试一次
- [ ] 确认外网**无法**直接访问 `:8000`、`:8001`

---

## 6. 常见问题

### 6.1 极空间不支持 `network_mode: host`

编辑 `docker-compose.tunnel.yml`，将 `cloudflared` 改为桥接网络并指向宿主机网关：

```yaml
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: cloudflared
    restart: unless-stopped
    command: tunnel --no-autoupdate run
    environment:
      TUNNEL_TOKEN: ${TUNNEL_TOKEN}
    extra_hosts:
      - "host.docker.internal:host-gateway"
```

同时将 Cloudflare Public Hostname 的 URL 改为：

- `http://host.docker.internal:8501`
- `http://host.docker.internal:8510`

若仍不通，改用极空间 **局域网 IP**，例如 `http://192.168.1.100:8501`。

### 6.2 Streamlit 页面异常 / WebSocket 断开

在 Cloudflare 控制台 → 对应域名：

- **SSL/TLS** 加密模式：**Full**
- **Speed** → 关闭 **Rocket Loader**（若有）

### 6.3 外网慢

主要受家庭/单位 **上行带宽** 限制；30 人不会同时在线时通常可接受。

### 6.4 更新版本

在 Windows 开发机：

```powershell
git pull
docker compose build
# 重新导出离线包，或仅 docker save 新镜像
```

极空间：`docker load` → `docker compose up -d`。

---

## 7. 安全上线检查表

- [ ] 修改所有默认密码（含 3267 管理员）
- [ ] Sub-I `.env` 服务账号使用强密码
- [ ] 后端 8000/8001 未暴露到 Tunnel / 路由器
- [ ] `.env.tunnel` 未泄露、未提交 Git
- [ ] 极空间快照 + 每周备份 `data/`、`uploads/`
- [ ] （可选）Cloudflare Zero Trust **Access** 限制邮箱白名单访问
- [ ] 与医院信息科确认业务数据允许外网访问

---

## 8. 本仓库相关文件

| 文件 | 作用 |
|------|------|
| `docker-compose.yml` | 四容器主配置 |
| `docker-compose.tunnel.yml` | 叠加 cloudflared |
| `.env.tunnel.example` | Tunnel Token 模板 |
| `start-tunnel.bat` | 启动四容器 + Tunnel |
| `导出离线部署包.bat` | 家里打包镜像与代码 |
| `离线部署.bat` | 极空间/离线机一键部署 |
| `备份数据.bat` / `恢复数据.bat` | 业务数据备份恢复 |

---

## 9. 费用参考

| 项目 | 月费用 |
|------|--------|
| 极空间 Z4 Pro | 0（已购） |
| 域名 | 约 3～5 元 |
| Cloudflare Free + Tunnel | 0 |
| **合计** | **约 3～5 元/月** |

---

## 10. 快速命令索引

```bash
# 仅业务容器
docker compose up -d

# 业务 + Tunnel（需已创建 .env.tunnel）
docker compose --env-file .env.tunnel -f docker-compose.yml -f docker-compose.tunnel.yml up -d

# 查看状态
docker compose ps
docker logs cloudflared --tail 50

# 停止 Tunnel
docker compose --env-file .env.tunnel -f docker-compose.yml -f docker-compose.tunnel.yml stop cloudflared
```

如有问题，可先查 `docker compose logs` 与 Cloudflare Tunnel 仪表盘中的 **Connector** 健康状态。