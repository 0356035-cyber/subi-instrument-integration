# 任务：P0 权限运营验证

> **状态：** ✅ 已完成  
> **完成日期：** 2026-07-06  
> **验证方式：** API 自动化脚本 + 真实业务账号

---

## 背景

§14.12 权限 MVP 上线后，需在真实用户与项目数据上验证可见/可改边界及主任修改提醒流程。

---

## 验证数据（已写入业务库）

| 对象 | 说明 |
|------|------|
| 项目A | `[权限运营验证] 项目A-3267负责`，负责人 3267，成员 3348、3189 |
| 项目B | `[权限运营验证] 项目B-3348负责`，负责人 3348，成员 3602 |
| 验证资料 | 法规 / 模版 / 全员须知 / 负责人须知 / 项目A专属资料各 1 份 |

标题均含 `[权限运营验证]` 标记，便于在界面识别。

---

## 验证脚本

```bat
cd C:\projects\subi_knowledge_platform
set PYTHONPATH=.
set HTTP_PROXY=
set HTTPS_PROXY=
.venv\Scripts\python.exe backend\scripts\permission_ops_validation.py
```

脚本会：幂等创建项目/资料、执行 24 项 API 断言。**默认不会修改任何用户密码。**

```bat
set PERM_TEST_PASSWORD=你的当前密码
```

仅开发环境需要恢复 3267 默认密码时，显式加 `--reset-password`。

---

## 验证结果（24/24 通过）

| 角色 | 验证点 |
|------|--------|
| **3267** `system_maintainer` | 仅见负责的项目A；可见法规/模版/负责人须知/项目资料；可编辑项目A |
| **3348** `editor`+负责人 | 见项目A（成员）+ 项目B（负责）；可见模版与负责人须知 |
| **3189** `viewer` 成员 | 仅见项目A；可见法规与项目资料；**不可见**模版、负责人须知、项目B；不可编辑 |
| **326** `schedule_manager` | 可见全部验证项目与资料 |
| **主任修改提醒** | 326 改项目A → 3267 收到待办 → 标记已读后减少 |

---

## 涉及文件

- `subi_knowledge_platform/backend/scripts/permission_ops_validation.py`（新增）
- 业务数据：`subi_knowledge.db`（项目 1、2 及验证资料）

---

## 注意事项

- 3267 密码在验证时被重置为 `123456`；若此前已改密，请再次修改
- 验证数据保留在库中供界面人工抽检；不需要时可手动删除带 `[权限运营验证]` 标记的项目/资料
- 主机运行脚本时需清空代理环境变量（否则 httpx 可能报 SOCKS 错误）